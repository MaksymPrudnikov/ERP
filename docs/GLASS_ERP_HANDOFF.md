# ERP для производства стекла — хендофф-документ

Версия 2.0 · 21 августа 2026 — внесены производственные ответы пользователя: размеры печи, уровни ЧПУ, роли, параметры расчёта цены, решение по серверу.

**Код:** https://github.com/MaksymPrudnikov/ERP · **Живая версия:** https://maksymprudnikov.github.io/ERP/
**Инструкция для внешнего ИИ (ChatGPT и др.):** `docs/ДЛЯ_ЧАТГПТ.md` — отдавать целиком перед постановкой задачи.
**Текущий план работ:** `docs/PLAN_2026-08-21.md` — этапы 3C…9 с критериями готовности и списком данных, которые нужны от пользователя. Отдавать вместе с этим документом.
Цель: полная замена Spil Glass (австралийский вендор), собственная система, код пишется блоками/модулями.

Это управляющий документ проекта. Отдавай его целиком любому LLM (ChatGPT, другой сессии Claude), чтобы продолжить работу без потери контекста. Обновляй по итогам каждой сессии.

---

## Stage 3B закрыт · 21 августа 2026

**PR #3 `stage3b-order-makeups` влит в `main` 21 августа, 06:31** (17 коммитов, 22 файла, +1248 / −79). CI зелёный, GitHub Pages обновился, живая версия показывает Stage 3B. Независимая проверка того же дня подтвердила состояние — см. раздел 9г.

Sales построен на модели **SalesOrder → order-scoped Makeups → Order Lines**. Глобальная Configuration Library отвергнута и не должна возвращаться. Makeup A/B/C локальны одному заказу; Shape и Muntin остаются существующими отдельными инженерными конфигураторами и привязываются к строке заказа через ссылки.

Makeup Builder: Single/Double/Triple; Vision/Spandrel/Laminated; Low-E/Reflective/Frit/Uncoated; независимые surfaces для coating/Frit/Spandrel; Lite/Cavity; spacer/gas/seals; laminated plies. Accordion UX: все Lite/Cavity закрыты по умолчанию, одновременно открывается только одна рабочая секция, collapsed row показывает summary. Canonical line dimensions хранятся в integer 1/16″. Текущий Master Data — стартовый seed и позже расширяется stock/order, suppliers, sheet sizes, lead time и costing. Детали реализации: `docs/STAGE3B_SALES_MAKEUPS.md`.

## 1. Контекст и рамки проекта

- **Кто:** владелец стекольного производства, использует Spil Glass ERP (Австралия), недоволен архитектурой типов стекла в текущей БД — считает её изначально неверной.
- **Цель:** не надстройка над Spil, а полная замена. Spil в итоге отключается совсем.
- **Текущий масштаб:** 10–100 заказов в день максимум, один-два цеха. При этом 18 авг 2026 пользователь одобрил расширенный целевой контур без архитектурного тупика: CPQ, MES/APS, полная genealogy, QMS, maintenance/energy, порталы, BI и будущий мультизавод. Это направление, а не требование внедрить всю сложность в первой фазе.
- **Роль разработчика:** ИИ (Claude) пишет код блоками/модулями постепенно. Пользователь не программист, работает через визуальную итерацию — смотрит готовый экран, а не код или SQL напрямую.
- **Развёртывание:** предпочтение — облако. Локальная копия на ПК допустима на старте, перенос на сервер позже. Часть операций (печать этикеток, выгрузка на станки) физически требует локального агента в цеху — облако до принтера/станка не достаёт.
- **Бюджет времени:** честная оценка полной замены — 15–25 месяцев, по фазам, каждая фаза даёт самостоятельную пользу.

### Ключевой принцип работы с пользователем
Пользователь учится через **визуальную итерацию**, не через код или SQL в чате. Он явно раскритиковал подход "получать сырые .sql файлы" — не может их использовать напрямую. **Каждая часть системы должна поставляться как рабочий HTML/браузерный прототип**, который открывается двойным кликом, без установки, без сервера. Сырой SQL — это внутренний рабочий материал для будущего переноса на сервер, не то, что показывается пользователю.

---

## 2. Что уже отвергнуто и почему (не предлагать снова)

| Отвергнуто | Причина |
|---|---|
| Миграция/патч текущей БД Spil | Архитектура типов стекла ошибочна с самого начала (закалка была зашита в тип стекла). Решено строить с нуля |
| Импорт клиентов Excel'ем на старте | Отложено — сначала один тестовый клиент + один тестовый тип стекла, клиенты грузятся позже |
| SQL-файлы как основной способ показать прогресс | Пользователь прямо сказал "зачем ты мне даёшь файлы SQL" — нужен рабочий интерфейс |
| Multi-select `<select multiple>` для выбора листов | Непонятно пользователю без Ctrl+клик — заменено на чекбоксы |
| Read-only прототип без возможности редактировать/удалять записи | Пользователь спросил "как потом манипулировать данными" — все сущности получили полный CRUD (добавить/изменить/удалить) с защитой от удаления используемых записей |
| Плоский список типов стекла ("6MM CLR TEMP" как одна строка) | Смешивает подложку, толщину, покрытие и обработку в одну строку — теряет связи, раздувается на сотни строк, не выводит остаток/маршрут/себестоимость |
| Группировка модулей "как удобно вводить" (материалы+покрытия+станки вперемешку) | Пользователь потребовал группировку **по бизнес-домену/владению**: склад/закупки/продажи/производство/отгрузка/финансы — каждый модуль отдельный, со своим отчётом, но связанный через ссылки |
| Хранить весь функционал Glass Configurator в одном большом файле | Частично готовые функции (мунтин ~80%, shape ~40%) мешают друг другу при правках. Решено: каждая специфическая способность — отдельный модуль кода с чётким входом/выходом, подключаемый к сервису (раздел 3, "Декомпозиция на независимые модули") |
| Слово "skill" в значении Claude Skill (SKILL.md/.skill) для этих модулей | Пользователь подтвердил — имел в виду обычный модуль кода, не формат Claude Skills. Не путать |
| **Глобальная Configuration Library** (общая на всю систему библиотека конфигураций стеклопакета) | Пользователь начал кликать конфигурации и увидел, что их сотни и тысячи. Комбинаторика делает библиотеку невозможной: по текущему сид-каталогу один Double = `(16 стекло × 3 закалки) × (8 spacer × 2 газа × 3 × 3 герметика) × (16 × 3)` = **331 776 сочетаний**, и это пока только 6 мм. С реальными толщинами, поверхностью покрытия, Frit, Spandrel, Laminated и Triple счёт идёт на миллионы. Заранее создать и назвать их нельзя — оператор утонет в поиске и наплодит дублей. Это та же болезнь, что и плоский список типов стекла «6MM CLR TEMP», только этажом выше. **Решение:** Makeup живёт внутри своего Sales Order и легко меняется; общая личность одинаковых пирогов даётся не хранимым каталогом, а вычисляемым fingerprint из id (см. раздел 5.4) |

---

## 3. Архитектурные решения (стек, платформа)

- **База данных:** PostgreSQL (Supabase или Neon).
- **Бэкенд + фронт:** TypeScript, Next.js. Монолит — не микросервисы, масштаб (10–100 заказов/день) не требует такой сложности.
- **Оптимизатор раскроя: НЕ пишем сами.** Решено интегрироваться с уже используемым **Perfect Cut** (R.O. SRL / Operations Research, perfectcut.glass) вместо разработки своего движка нестинга — это снимает самый тяжёлый и рискованный кусок фазы 3.
  - У пользователя только базовый Perfect Cut, без модулей Perfect Label / Perfect Code / Perfect Stock.
  - Печать стикеров/этикеток уже решена — Perfect Cut печатает их сам на принтере (TCL/TSC). Наш локальный агент печать этикеток НЕ обслуживает.
  - Трекинг детали по станциям цеха и учёт остатков/обрези по-прежнему наши — Perfect Code (штрихкоды) и Perfect Stock (остатки) у пользователя не куплены.
  - **Механизм обмена ещё не подтверждён.** Со слов пользователя: "по нажатии кнопки Perfect Cut получает доступ к батчу" — похоже на прямое подключение к БД Spil (SQL Server) или настроенный коннектор R.O. SRL под Spil, а не на ручной файловый экспорт. План: пользователь посмотрит настройки подключения в Perfect Cut (DSN/строка подключения к Spil) и/или спросит у R.O. SRL, как именно настроен этот коннектор — прямая БД, ODBC или файл, и в каком формате. До получения этой информации формат обмена не проектируется — угадывать его бессмысленно.
  - Локальный агент в цеху меняет назначение: раньше планировался под печать этикеток + выгрузку DXF на станки, теперь его роль — быть мостом данных между нашей БД и Perfect Cut (передать батч на оптимизацию, забрать результат раскроя обратно для трекинга и учёта остатков).
- **Цеховые терминалы:** PWA с офлайн-очередью (IndexedDB), сканирование пишется локально и досылается при восстановлении сети.
- **Локальный агент в цеху:** отдельный маленький сервис на ПК, его подтверждённая текущая роль — **мост данных между облачной ERP и локальным Perfect Cut**. Печать этикеток оставлена Perfect Cut; формат/механизм обмена пока не проектировать до получения реальных настроек коннектора Spil ↔ Perfect Cut. Выгрузка на другие станки может потребовать локальный компонент позже, но конкретный протокол не придумывать заранее.
- **Хостинг:** Vercel/Fly + managed Postgres + ночной дамп на локальный NAS как резервная копия (не второй источник правды).
- **Переезд на сервер отложен осознанно (решение пользователя, 21 авг 2026).** Дословно: «пока хранить только в браузере для тестового образца на поклацать». Вместо переезда пользователь заказал **экран работы с базой** — чтобы своими руками полазить по данным и сказать, чего в модели не хватает и что лишнее. То есть Postgres не раньше, чем модель будет им же принята на прототипе. Признак, что тянуть больше нельзя, остаётся прежним: двум людям нужно работать одновременно, либо потеря браузерных данных станет неприемлемой.
- **Бухгалтерия НЕ переписывается.** AR/AP, GL, payroll, tax, cash book, fixed assets остаются во внешней системе (QuickBooks/Xero/Sage). Своя ERP только выставляет счёт и передаёт данные наружу. Это решение экономит ~год работы и снимает регуляторный риск.

### Прототипирование — текущий подход
Каждый модуль сначала строится как **самодостаточный HTML-файл** (inline CSS/JS, без зависимостей, открывается локально), с:
- Полным CRUD по всем сущностям (добавить / изменить / удалить с защитой от удаления используемых записей)
- Экспортом/импортом состояния в JSON (кнопки в шапке) — так пользователь сохраняет свою работу между сессиями и присылает её обратно для переноса в реальную БД
- Автосохранением в localStorage браузера
- Валидацией на вводе (обязательные поля, дубликаты, диапазоны)
- Живыми расчётами (проверка маршрута, цена детали) — не просто справочники, а рабочая логика

Когда модель устаканится на прототипе — те же самые экраны переезжают на реальный Next.js + Postgres, меняется только источник данных.

### Декомпозиция на независимые модули (решение от авг 2026)

Пользователь сформулировал принцип: не писать всю функциональность в один файл/модуль, а разбивать каждую специфическую способность (требующую своей геометрии, чертежей, расчётов) на **отдельный модуль кода** — и уже потом подключать его к кнопке или сервису внутри системы.

**Важно про терминологию:** пользователь употребил слово "skill", но явно подтвердил (авг 2026, при уточнении) — имеется в виду **обычный программный модуль**, НЕ формат Claude Skill (SKILL.md/.skill). Не путать эти два понятия при дальнейшей работе. В документе и коде использовать слово "модуль", не "skill", чтобы не создавать путаницу с системой Skills этой платформы.

**Технический контракт модуля:**
- Один файл/папка на одну способность (например `muntin.js`, `free-shape.js`).
- Чёткий вход/выход: модуль получает данные (например размеры камеры, параметры сетки) и возвращает результат (геометрия, DXF-сегменты, BOM) — без обращения к остальной системе напрямую.
- Модуль не знает про цены, клиентов и заказы — эта развязка инженерной логики (геометрия/DXF) от бизнес-логики (цена/клиент) сделана специально: так модуль можно чинить, тестировать и потенциально показать/предложить/продать отдельно, не утаскивая за собой закрытые бизнes-данные.
- Точка подключения — сущность `service` из модели данных (раздел 4.2): у сервиса типа "мунтин" может быть привязан конкретный модуль-калькулятор. Это совпадает с уже существующей моделью услуг в ERP, отдельного нового механизма подключения выдумывать не нужно.

**Решение по конкретным функциям Glass Configurator (авг 2026):** Muntinbar и Shape **переносятся из существующего Configurator, а не переписываются с нуля**. Для текущего ERP-кандидата уже подключена реальная логика **Smart-Shape / Advanced из v4.5** и shape-adaptive **Muntin Production**. При переносе не исправлять известные геометрические баги попутно: сначала сохранить существующее поведение и контракт, затем чинить внутри изолированного модуля отдельными изменениями.

**Критическая связь Shape → Muntin:** Shape является единственным источником размеров и контура детали. Пользователь сначала создаёт/выбирает Shape (Width/Height + Advanced geometry), затем Muntinbar выбирает этот Shape и рассчитывает сетку/длины баров по его реальному контуру. **Не добавлять отдельные Width/Height в Muntinbar** — это создаст второй источник геометрии и риск расхождения.

### Workflow разработки: коммиты напрямую из облака (решение от авг 2026)

Пользователь выбрал: **коммитить код напрямую из этой облачной сессии через GitHub personal access token**, а не через свой подключённый компьютер и не оставаться только на файлах без версионирования.

Согласованная схема:
- Пользователь сам создаёт пустой репозиторий на GitHub (создание репо через токен не даём — токен не должен иметь права на создание репозиториев).
- Токен — **fine-grained PAT**, привязанный к ОДНОМУ этому репозиторию, права только **Contents: Read and write**, короткий срок действия (не "no expiration").
- Токен передаётся в чат сессии и живёт только в её рамках (session-ephemeral) — не хранится в коде, не коммитится.
- Для архитектурных этапов вроде Stage 3B использовать отдельную feature-ветку и сначала получить зелёный CI. В `main` переносить только проверенный кандидат. Небольшие безопасные исправления после merge могут идти напрямую в `main`. Ветка `stage3b-order-makeups` влита как PR #3 21 августа и больше не нужна.
- **Осторожно с CI на ветке:** в `.github/workflows/build.yml` сейчас триггер только `push: branches: [main]` + `workflow_dispatch`. Feature-ветку CI сам не проверяет — `stage3b-order-makeups` проверялась ручным запуском через вкладку Actions. Пока триггер не расширен (Этап 3C плана), «зелёный CI перед merge» нужно получать руками, иначе можно влить непроверенное.
- **Сделано (16 авг 2026):** репозиторий `https://github.com/MaksymPrudnikov/ERP` создан пользователем, код залит — 12 коммитов, 41 файл.
- **Важно про способ заливки:** прямой `git push` из облачной сессии НЕ проходит. У сессии свой git-прокси, он пускает только к репозиториям, добавленным в «sources» задачи, и присланный в чат PAT игнорирует. Первая заливка сделана через веб-интерфейс GitHub браузером пользователя (Claude in Chrome), по одному коммиту на папку. Токен так и не понадобился.
- **Подтверждено повторно (17 авг 2026):** тот же обходной путь — веб-загрузка файла через `Add file → Upload files` (drag-and-drop) в нужную папку репозитория, а не правка через встроенный CodeMirror-редактор GitHub — использован для двух коммитов по `muntin/drawing.js` (см. раздел 9а). Прямое редактирование файла через веб-редактор GitHub (клик в код → Ctrl+A → печать нового содержимого) на этой сессии оказалось ненадёжным: редактор подвисал на объёме текста с кириллицей и один раз оставил в файле дублированный кусок старого + нового содержимого. **Для замены файла целиком использовать именно загрузку файла (Upload files), не правку в браузерном редакторе.**
- **Чтобы следующая сессия могла пушить сама** — при её запуске добавить `MaksymPrudnikov/ERP` в источники задачи. Иначе снова только браузер или `git push` руками с машины пользователя.
- Если работать из **Claude Code** (CLI-инструмент, не облачная Cowork-сессия) — этого ограничения нет, `git push` идёт напрямую своими git-учётными данными, обходной путь через браузер не нужен. Ограничение прокси касается только облачных Cowork-сессий этого типа.
- `device_bash` на машине пользователя работает БЕЗ сети — git-операции оттуда невозможны.
- SQL-миграции 01–05 в этот репозиторий ещё НЕ залиты — их нужно донести отдельным коммитом в `schema/` (файлов не было в сессии переноса).

### Структура репозитория (решение от авг 2026) — модули отдельными файлами

Пользователь потребовал прямо: **модули отдельно, и подмодули внутри модулей тоже отдельно**, чтобы
чинить по одному и добавлять версии в git, а не пересобирать большой HTML-файл руками.

```
src/core/            dim.js · poly.js
src/modules/shape/   consts · model · contour · validate · geometry · index
src/modules/muntin/  catalog · model · layout · clip · adaptive · bom · drawing · index
src/erp/             data · icons · i18n · nav · storage + views/*
build/build.js       → dist/GLASS_ERP.html (один файл, открывается двойным кликом)
test/run.js          регрессионные тесты геометрии и оболочки
```

- `dist/GLASS_ERP.html` — то, что пользователь скачивает и открывает. **Собирается скриптом, руками не редактируется.**
- `src/index.html` — та же система, модули подключены отдельными `<script src>`; правишь модуль → F5, без сборки.
- Разбиение сделано **чистым переносом текста**: собранный файл байт-в-байт совпал с исходным кандидатом по коду (проверено сравнением и одинаковыми числами геометрии в трёх сборках).
- Правило: **правку вносить в один маленький модуль, потом `node build/build.js`.** Полный HTML заново не собирать.

---

## 4. Ядро модели данных — материалы и стекло

### 4.1 Главный принцип: разделение осей
**Материал = подложка + толщина + покрытие.** Обработка (закалка) НЕ входит в материал — на складе закалённого стекла не бывает, закалку делает сам цех. Название и вес не хранятся полями — вычисляются из связанных таблиц.

Было неправильно: `6MM CLR TEMP` как одна строка справочника.
Стало правильно: `substrate(CLR) + thickness(6mm) + coating(null) → material`, а `treatment(TEMP)` — отдельная сущность, применяется к детали в процессе, не к материалу на складе.

### 4.2 Таблицы (реализованы в SQL-миграциях 01–05)

**`substrate` (подложка)** — тело стекла как сварил завод. БЕЗ толщины, покрытия, обработки.
- `code`, `name`, `family` (clear/low_iron/tinted/patterned/wired/other)
- `tint_colour_code` (FK, обязателен только для tinted)
- `density_kg_m3` (нужен для веса детали: вес = плотность × толщина × площадь)
- `is_temperable`, `is_laminatable`, `is_directional` (узорчатое стекло имеет лицевую сторону)
- `legacy_code` (маппинг на старые артикулы Spil)
- CHECK: tinted обязан иметь tint_colour; wired не может быть temperable (физическое ограничение)

**`coating` (покрытие)** — покрытие как продукт. Номер поверхности (#2, #3, #4) — свойство СЛОЯ ИЗДЕЛИЯ, не покрытия.
- `coating_type` (low_e/solar_control/reflective/mirror/self_cleaning/anti_reflective/etch)
- `deposition` (pyrolytic твёрдое / sputtered мягкое / chemical / applied)
- `applied_by` (primary_manufacturer / offline_coater / **fabricator** — если наносим сами, это генерирует шаг маршрута, а не позицию закупки)
- `temper_mode` — **тройное состояние, не булево**: `annealed_only` / `temperable` / `temper_required` (свойства раскрываются только после закалки)
- `exposure_rule` (any/cavity_only/exterior_only/interior_only) — рабочий пример: **i89 от Cardinal работает на поверхности #4, внутрь помещения**, а не в камеру, в отличие от всех остальных low-E. Это ловится constraint'ом `coating_mirror_rules` и полем exposure_rule.
- `requires_edge_deletion` + `edge_deletion_width_mm` (согласованы constraint'ом)
- `emissivity`, `shelf_life_days` (мягкие покрытия окисляются, партия имеет срок годности)
- Правило разделения покрытие vs услуга: *если две партии взаимозаменяемы и могут лежать на одной стойке — это покрытие; если задаётся под конкретный заказ (рисунок, %, цвет) — это услуга.* Шелкография = услуга (`line_service`), зеркало/сатин = покрытие (`applied_by = offline_coater`).

**`thickness` (толщина)** — номинал ≠ факт. "6мм" это обозначение, не измерение.
- `nominal_mm`, `imperial_label`, `actual_mm`, `is_verified`
- **Важно:** фактическая толщина в итоге переехала на уровень `material`, а не осталась только в `thickness` — потому что разные линии дают разный факт при одном номинале (6mm Clear = 5.77мм, 6mm LoE-272 = 5.70мм). `material.actual_thickness_mm` перекрывает значение из `thickness.actual_mm`.

**`material`** — единица закупки/склада. `substrate_id + thickness_id + coating_id(nullable) → уникальная связка`. Плюс `actual_thickness_mm`, `thickness_verified`, `legacy_code`.

**`supplier` / `source` (точка поставки)** — отделены друг от друга. Один поставщик может иметь несколько точек поставки с РАЗНОЙ ВАЛЮТОЙ: Vitro Barrie в CAD, Vitro USA в USD, Cardinal USA в USD. Валюта принадлежит точке поставки, не поставщику.

**`sheet_size` (типоразмер листа)** — зависит от ПАРЫ материал+поставщик, не глобален. Vitro продаёт 144×100", Cardinal тот же условный "144" даёт как 144×96". Таблица связи `material_supply_sheet`.

**`treatment` (обработка)** — закалка/HS/отжиг как процесс.
- `is_heat_treated`, `requires_furnace`
- **`can_cut_after`** — главное поле: false у закалки/HS/HST. Отсюда следует весь порядок маршрута: рез, кромка, отверстия — всё ДО печи, размер реза = финальный размер.
- `strength_factor` (HS ≈2x, tempered ≈4x относительно annealed)

**`work_center` (станок/рабочий центр)** — физическое ограничение оборудования.
- `max_width_in`, `max_length_in`, `min_width_in`, `min_thickness_mm`, `max_thickness_mm`
- Заведено: печь закалки `FURN1` (90×150"), кромкообработка `EDGE1`, фацетный станок `BEVEL1` (70×100"), ЧПУ `CNC1` (60×122")
- **ЧПУ — это две разные операции на разных уровнях маршрута** (ответ пользователя, 21 авг 2026): *CNC shape polishing* — **уровень 2** (кромка/полировка), *CNC processing* — notch, radius и прочее — **уровень 3** (сверловка/выемки). Значит один физический станок появляется в маршруте **дважды, на разных шагах**. Заводить либо как два рабочих центра (`CNC1-POL` ур. 2 и `CNC1-PROC` ур. 3) с общим габаритным ограничением 60×122", либо разрешить одному work_center участвовать в двух уровнях. Модель обязана это допускать — иначе маршрут детали, у которой есть и полировка контура, и вырез, построится неверно.
- **Ключевой инсайт:** максимальный размер детали — НЕ константа, а функция от МАРШРУТА через набор станков. Одна и та же деталь может проходить через печь, но не проходить фацетный станок (разные лимиты). Функция `check_route_fits()` / `checkRoute()` в JS-прототипе проверяет весь список операций разом и возвращает все нарушения.
- Печь 90×150" не берёт лист 130×96" целиком (96 > 90 по короткой стороне) — целый лист закалить нельзя, только раскроенные детали.

**`service` (услуга)** — притупление кромки, полировка, фацет, сверловка, ЧПУ.
- `stage`: pre_temper (вся механика — закалённое стекло не обрабатывается) / post_temper / any
- `affects_size` — операция съедает материал, размер реза больше финального
- `service_allowance` — припуск на рез, зависит от услуги И толщины, отдельно ширина/высота (SEAM 0, POLISH 1/16", BEVEL 1/8", CNC 1/4" — для 6мм)
- `service_work_center` — связь услуга↔станок, лимиты можно переопределить на уровне связки

### 4.3 Данные пользователя (реальные, уже введены)
- **Vitro** (основной поставщик): clear 3–19мм + Solarban 60/70/90. Листы 130×96" и 144×100". Точки поставки: Vitro Barrie (CAD), Vitro USA (USD).
- **Cardinal** (второй поставщик): только coated — i89, LoE-180/272/366. Листы 130×96" и 144×96". Точка поставки: Cardinal USA (USD).
- Импортное стекло — добавится позже, пока не задано.
- Печь закалки: рабочее поле **90 × 150 дюймов**. **Минимальный размер детали — 6 × 12″** (ответ пользователя, 21 авг 2026). **Максимальный вес садки точно не известен:** пользователь оценил его как «полная садка 15 мм стекла 90 × 140″», что даёт ≈ **305 кг** (2500 кг/м³ × 0.015 м × 8.13 м² = 304.8 кг ≈ 672 lb); для полного поля 90 × 150″ та же прикидка даёт ≈ 327 кг. **Это оценка пользователя, а не паспорт печи** — проверить по документации до того, как ставить лимит в планирование садок, иначе Ф4 будет строить план на выдуманном ограничении.
- Фацетный станок (beveling): **70 × 100"**.
- ЧПУ (CNC): **60 × 122"**.
- Фактические толщины: 6mm Clear = **5.77мм**, 6mm Cardinal LoE-272 = **5.70мм**.
- Два базовых тестовых типа засеяны: (1) 6mm Clear Vitro без покрытия, (2) 6mm Clear + LoE-272 Cardinal.
- Тестовый контур для всей системы: **монолит закалённый** (не IGU, не триплекс — самый простой случай для обкатки модели).

---

## 5. Ценообразование (только что спроектировано, нужно доработать)

### 5.1 Бизнес-правила от пользователя
- Модель: **закупка по себестоимости → своя наценка → плюс услуги → плюс energy surcharge**.
- Минимальная площадь к оплате: **4 ft²** по умолчанию, но может быть **2 или 3 ft²** для отдельных случаев (переопределение на уровне материала или клиента).
- Округление размера для расчёта площади к оплате: **вверх до целого дюйма** (23.25" → 24"). Это отдельно от размера реза/себестоимости, который считается по факту с припусками.
- Валюта закупки зависит от точки поставки (см. `source` выше), не от материала напрямую.
- **Профит виден ТОЛЬКО владельцу.** Уточнено пользователем 21 авг 2026. Раньше в этом документе стояло «админ и специально допущенные» — **это исправлено: админ теперь роль техническая и прибыль по умолчанию НЕ видит.** Право видеть прибыль привязывать к конкретному пользователю-владельцу, а не к админской роли, иначе оно утечёт вместе с админскими правами. Это касается всей цепочки: цена закупки, точка поставки, фрахт, курс, себестоимость, маржа/наценка в процентах, отчёты P&L. Наценка в процентах тоже скрывается — иначе через неё вычисляется себестоимость. Скрытие должно быть на уровне ВЫДАЧИ ДАННЫХ (API/отчёт/экспорт), не только на уровне экрана — иначе Excel-экспорт или печатная форма всё равно унесёт цифры.

### 5.2 Что уже реализовано в прототипе v3
- Разделение: **фактическая площадь** (для себестоимости, с припусками на услуги + % отхода) vs **площадь к оплате** (округлённая, с учётом минимума).
- `costOf(material)` — тянет последнюю по дате цену закупки, конвертирует по курсу FX в базовую валюту, добавляет % фрахта.
- `priceLine()` — полная раскладка: размер после округления → площадь факт → площадь к оплате → размер реза с припусками → расход с отходом → себестоимость → цена продажи (наценка) → построчно услуги/обработка по их ценам → подытог → energy surcharge → итог за деталь → итог × количество.
- Таблицы: `fx` (курсы), `cost` (цены закупки по материалу+точке поставки+дате), `servicePrice`, `treatPrice`, `settings` (базовая валюта, округление, минимум ft², % отхода, % наценки, % energy surcharge).

### 5.3 Ещё не сделано / открытые вопросы по цене
- Права видимости профита (роль/пользователь → что скрыто) — спроектировано словами, не реализовано в коде.
- Цены/скидки по конкретным клиентам, скидки от объёма, надбавка за срочность — отложено до модуля заказа/клиента.
- Реальные цены закупки — пользователь распорядился (21 авг 2026) поставить временно **1.00 по всем позициям** вместо нуля, чтобы цепочка расчёта считалась и была видна целиком; реальные цены впишет в базу позже, по каждому товару отдельно. **Определить единицу явно** — 1.00 за ft² материала, не за лист и не за деталь, иначе placeholder разъедется с реальными ценами при замене.
- Курс USD→CAD — **1.37 подтверждён** пользователем 21 авг 2026. Всё равно держать в таблице `fx`, а не константой в коде: курс меняется, и старые заказы должны считаться по курсу своей даты.
- **Модель цены из v3 в новую оболочку не перенесена.** В схеме Sales Order есть валюта и условия оплаты, но нет ни одного поля с суммой. Перенос — Этап 6 плана, критерий приёмки: реальный заказ из Spil воспроизводится с точностью до центов.

### 5.4 Отпечаток Makeup вместо библиотеки конфигураций (решение от 21 авг 2026)

Глобальная библиотека конфигураций отвергнута по комбинаторике (раздел 2). Но у order-scoped Makeup есть цена этого решения, и её надо закрыть осознанно: **одинаковый пирог в двух заказах — это две разные записи с разными id**. Следствия: нельзя спросить «сколько таких стеклопакетов мы сделали за год», нельзя вести себестоимость и цену по пирогу, постоянный клиент каждый раз собирает свой стандартный пакет заново.

**Решение — не каталог, а вычисляемый ключ.** `makeupFingerprint(m)` считается из **id** (стекло, закалка, поверхность покрытия, spacer, газ, герметики — в фиксированном порядке), а не из названий, ровно как `fingerprint: shp-…` уже сделан для Shape. Хранить каталог не нужно: одинаковые makeup в разных заказах схлопываются сами, и на этом строятся отчёты и себестоимость по пирогу.

**Не горит.** В отличие от журнала событий, отпечаток вычисляется из данных, которые уже сохранены (makeup хранит именно id, а не тексты), — значит его можно посчитать задним числом по всем старым заказам. Поэтому идёт вместе с ценообразованием, Этап 6.

**Следствие для справочников:** раз Makeup ссылается на продукт по id, позицию каталога **нельзя удалять** — старый заказ покажет `?` вместо кода стекла (`salesGlassCodeForPane` подставляет `'?'`, если продукт не найден). Только пометка «неактивен». Заложено в Этап 5 плана.

### 5.5 Подтверждённые параметры расчёта (ответы пользователя, 21 авг 2026)

Всё это — **значения по умолчанию в таблице `settings`, а не константы в коде.** Пользователь отдельно потребовал возможность менять отход и energy surcharge.

| Параметр | Значение | Комментарий |
|---|---|---|
| Курс USD → CAD | **1.37** | подтверждён; хранить в `fx` с датой, не константой |
| Минимальная площадь к оплате | **4 ft²** | подтверждена как значение по умолчанию; переопределение 2 или 3 ft² на уровне материала/клиента остаётся в модели, конкретные случаи пользователь ещё не назвал |
| Округление размера для площади к оплате | вверх до целого дюйма | без изменений, раздел 5.1 |
| Процент отхода | **15 % минимум** | пользователь: «пусть будет возможность изменять» — значит поле в настройках, а не число в формуле |
| Energy surcharge | **8.75 %, до налога** | пользователь сначала сказал «после налога», затем поправил себя: **до налога**. Ставка может меняться — значит поле в настройках |
| Цена закупки | **1.00 за ft²** временно | placeholder вместо нуля, чтобы цепочка расчёта была видна целиком |

**Energy surcharge начисляется ДО налога** — подтверждено пользователем 21 авг 2026 отдельной поправкой. Это совпадает с порядком расчёта из раздела 5.2: «подытог → energy surcharge → налог → итог». Менять порядок не нужно, только вынести ставку 8.75 % в настройки, потому что она плавает.

---

## 6. Модульная структура (актуальное решение — переработать группировку)

Пользователь потребовал: **модули по бизнес-домену/владению**, не по "удобству ввода". Каждый модуль отдельный, но связан через ссылки, и у каждого должен быть СВОЙ ОТЧЁТ (что/как/сколько делалось).

| Модуль | Владеет данными | Отчёт |
|---|---|---|
| **Склад / инвентаризация** | Номенклатура, материалы, подложки, покрытия, толщины, листы; остатки, партии, обрезь, движения | Расход, оборачиваемость, что лежит мёртвым грузом |
| **Закупки** | Поставщики, точки поставки, цены закупки, заказы поставщику, приёмка, MRP | Динамика цен, поставки в срок, недопоставки |
| **Продажи** | Клиенты, прайсы, скидки, котировки, заказы, ACK | Выручка, маржа по продукту (скрыто не всем), конверсия котировок |
| **Производство** | Станки, услуги, маршруты, план, детали в работе, бой и перезаказ | Выработка, загрузка станков, брак и причины |
| **Отгрузка** | Стойки, комплектация, маршруты, накладные | Отгружено в срок, простой стоек |
| **Финансы** | Фактическая себестоимость, счета, интеграция с бухгалтерией | P&L по продукту, где теряется маржа |

**Ядро (общее для всех модулей, не отдельный модуль):**
- Единицы измерения, валюты, курсы
- Пользователи и права
- **Журнал событий** — критично: отчёты "что как сколько делалось" читаются ИЗ ИСТОРИИ СОБЫТИЙ, не из текущего состояния таблиц. Текущее состояние БД не хранит "сколько закалили в июле" — если события не писать с первого дня, задним числом это не восстановить. Каждое действие в любом модуле обязано писать событие: кто, что, когда, сколько, над каким объектом.

**Важно:** прототип v3 (файл glass_erp_v3.html) сгруппирован ПО-СТАРОМУ — вкладки "Материалы / Каталоги / Производство / Закупка / Продажа / Расчёт". Это НЕ переделано и остаётся как справочный прототип модели материалов/цены, не как основа для дальнейшей доменной структуры.

**Доменная оболочка построена и живёт в репозитории** — состав экранов и что в них реально работает см. раздел 10. Кратко: Dashboard, Users, Customers, Sales (Draft Sales Orders + Makeup Builder), Configurators (Production Shape + Adaptive Muntin), Optimization, Production. Домены Inventory / Purchasing / Shipping / Finance пока только в меню.

**Чего в оболочке ещё нет из требований этого раздела:**
- журнала событий — а без него отчёты «что как сколько делалось» построить нельзя задним числом;
- собственных отчётов у каждого домена;
- модель материалов и цены (разделы 4–5) в новую оболочку не перенесена, остаётся в справочном `glass_erp_v3.html`.

---

## 7. Права доступа (спроектировано словами, не реализовано)

- **Сейчас:** только админ (стадия разработки).
- **В перспективе:** разделение по модулям, для каждого — просмотр vs изменение.
- **Уровней не два, а четыре:** нет доступа / просмотр / изменение / **согласование** (например кладовщик может списать бой, но не может утвердить списание — разные права на одно действие).
- **Права нужны и на уровне полей, не только модулей.** Пример: продажник видит цену продажи, но не видит себестоимость и маржу — это одна колонка внутри ЕГО ЖЕ экрана заказа, не "доступ к модулю Финансы" целиком.
- **Профит закрыт по умолчанию** — только админ + явно допущенные пользователи (см. раздел 5.3).
- **Архитектурное решение:** одна точка проверки прав — функция вида `можно(роль/пользователь, модуль, действие)`, вызываемая при КАЖДОМ действии. Не размазывать проверки по интерфейсу — иначе разделение прав станет отдельным проектом на месяц вместо правки одной таблицы.
- **Проверка должна быть на уровне выдачи данных (API), не на уровне UI** — спрятанная кнопка/колонка не защита, данные всё равно приедут в браузер через тот же запрос.
- **Цеховые терминалы — особый случай:** учётка принадлежит станции/терминалу, а не человеку; конкретный оператор идентифицируется сканом бейджа при действии. Если заставлять логиниться/разлогиниваться на каждой смене — операторы будут работать под одной общей учёткой, и журнал событий (см. раздел 6) станет бесполезным.
- **Реальные роли названы пользователем (21 авг 2026): `Продажи` · `Бухгалтер` (внутренний, по заказам) · `Админ` · `Владелец`.** Четыре, а не семь. В коде сейчас засеяно `['Админ','Руководство','Продажи','Технолог','Цех','Отгрузка','Снабжение']` (`src/erp/data.js`) — **список надо привести к реальному**, иначе права проектируются под несуществующие должности.
- **Прибыль видит только `Владелец`** (раздел 5.1). `Админ` — техническая роль: настройки, справочники, пользователи, но не деньги.
- **Цех, отгрузка и снабжение как именованные пользователи пока не заводятся.** Пользователь их не назвал, и это согласуется с решением выше по цеховым терминалам: учётка принадлежит станции, а конкретный рабочий опознаётся сканом бейджа при действии. Если это прочтение неверно и в цеху нужны личные учётки — переспросить, не додумывать.

---

## 8. Дорожная карта по фазам (из первоначального плана v1, актуальна)

| Фаза | Срок | Содержание | Критерий выхода |
|---|---|---|---|
| **Ф1. Фундамент** | 1–2 мес | Мастер-данные, каталоги материалов (в процессе — см. раздел 4) | Мастер-данные совпадают со Spil по контрольным выборкам |
| **Ф2. Заказ** | 2–4 мес | Клиенты, заказ (шапка+строки+услуги), прайсинг, разузлование строки в физические компоненты, ACK, чертёж | Параллельный ввод в Spil и у нас 2 месяца — расхождений по цене и составу деталей нет |
| **Ф3. Цех** | 4–7 мес (короче — оптимизатор не пишем) | Мост данных к Perfect Cut (наша БД → батч → результат раскроя обратно), сканирование/WIP, бой/перезаказ, учёт остатков и обрези | Batch из нашей БД оптимизируется в Perfect Cut так же, как раньше из Spil, результат раскроя корректно попадает в трекинг и склад |
| **Ф4. Планирование** | 4–6 мес | Календарь мощностей, садки закалки, партии ламинации/линии СП, стойки, маршруты доставки | План на неделю строится без ручного вмешательства |
| **Ф5. Замыкание** | 4–6 мес | Склад/MRP, фактическая себестоимость, интеграция с бухгалтерией, рекламации/NCR, BI, портал клиента, отключение Spil | Месяц работы без единого входа в Spil |

**Состояние на 21 августа 2026: Ф1 закрыта по содержанию, начата Ф2.** Мастер-данные и доменная оболочка построены, Customer Master и Draft Sales Orders работают, чертёж есть. В Ф2 не сделано: **прайсинг, разузлование строки в компоненты, ACK**. Плюс из Ф1 остались хвосты: экран справочников (каталог правится только через JSON) и заполнение реальных цен пользователем. Порядок работ — `docs/PLAN_2026-08-21.md`.

Модель данных для материалов в целом устоялась (миграции 01–05), но требует: (а) переноса экранов материалов/цены в новую оболочку, (b) прав доступа, (c) заполнения реальных цен пользователем.

### Модель данных для СЛЕДУЮЩЕГО модуля (заказ, компонент) — ядро, которое ещё нужно спроектировать
```
customer / site / contact
order / order_line        форма, размеры, кол-во, срок, мейкап
  line_service             кромка по 4 сторонам, ЧПУ, сверловка и т.д. (уже частично есть в service/allowance)
  line_shape                контур: линии и дуги, свободная форма
component                 РЕЗУЛЬТАТ РАЗУЗЛОВАНИЯ — физическая деталь (одна строка стеклопакета = N компонентов)
  routing_step              маршрут детали по станциям (используя work_center из раздела 4)
  component_event           скан на станции, время, оператор, статус (это и есть журнал событий)
batch                      садка закалки / партия автоклава / партия линии СП
```
**Важно:** `component` ≠ строка заказа. Одна строка триплекса — это 3–4 компонента, каждый со своим маршрутом. Если этого нет в модели с первого дня — переписывать придётся всё.

---

## 9. Существующие активы пользователя (связанные проекты)

- **Glass Configurator** (он же "IGU Builder") — исходный single-file HTML app пользователя: shapes, hardware, adaptive muntin, shop drawing, DXF export. В проде нигде не применяется; разросся в один большой файл, поэтому инженерные функции вынесены из него в независимые модули.
  - **Авторитетный источник переноса Shape:** `glassconfigurator-v4.5-smartshape_4(1).html` + `SMART-SHAPE-Advanced(1).md`. Smart-Shape / Advanced: A/B/C/D, elbows, corner blocks, единый contour, собственная validation-модель.
  - **Авторитетный источник переноса Muntin:** production geometry из того же Configurator. Спецификация мунтина остаётся отдельной, но фактические cut lengths считаются по реальному perimeter выбранного Shape, с production grid 1/16″ и edge-reference логикой.
  - **Перенос выполнен и лежит в репозитории** — `src/modules/shape/*` и `src/modules/muntin/*`, разрезанные на подмодули (см. раздел 10). Геометрия перенесена дословно.
  - **Связь:** Shape → Muntin. Shape хранит размеры и контур; Muntin хранит свою спецификацию и ссылку `shapeId`, но НЕ дублирует размеры стекла.
  - Известные ограничения исходной Smart-Shape / Muntin документации не считать исправленными только потому, что модуль перенесён. Ревьюить и тестировать отдельно перед продакшеном.
- Также есть отдельные HTML-формы: IGU spec sheet, силикскрин (silk screen), spandrel (крашеный спандрель) — потенциально части модуля продаж/спецификации заказа.

### 9а. Обновления чертежа Muntin — `drawing.js` (17 авг 2026)

Пользователь передал две последовательные версии `src/modules/muntin/drawing.js`, разработанные внешним ИИ (ChatGPT), для заливки в репозиторий. Обе прошли `node --check` и полный прогон `node test/run.js` (38/38) до заливки, `dist/GLASS_ERP.html` пересобран после каждой, содержимое на GitHub сверено побайтово (`diff`) с локально проверенной версией.

**Коммит 1 — "production setout" (контрастные цвета).**
- Для прямоугольной детали: вместо размеров V1/V2/V3.../H1/H2/H3... от внешнего края до ЦЕНТРА бара — накопительные установочные размеры от линии Edge Inset до БЛИЖНЕЙ ГРАНИ каждого бара (то, что реально нужно у станка/на разметке).
- Цвета сделаны контрастнее: Edge Inset — тёплый янтарный (`#b36b00`), чистые просветы (Clear Opening/DLO) — тёмно-синий (`#0057b8`) вместо прежнего светло-голубого, установочные размеры — нейтральный тёмно-серый.
- Логика для Shape (фигурных деталей) в этом коммите не поменялась — оставлена старая координатная привязка V1/V2/H1/H2.

**Коммит 2 — аудит ГПТ, размеры для Shape.** Второй заход, представленный пользователем как «провёл аудит и исправил все ошибки». Основная часть изменений — именно про фигурные (Shape) детали, где раньше не было честной привязки к реальному контуру:
- **Edge Inset для Shape** теперь строится как настоящий offset-контур исходного periметра (функция `offsetData`), а не показывался вообще — линия идёт параллельно реальному, возможно наклонному/ступенчатому краю детали, а не по bounding box.
- **Чистые просветы (синим)** для Shape считаются сканированием реального контура по горизонтали/вертикали (`scanIntervals`) — то есть число соответствует фактическому проёму в этом месте детали, а не приближению по прямоугольнику.
- **Установочные размеры (setout)** для Shape строятся от реальной кромки (через `shapeHorizontalRef` / `longestVerticalEdge`), а не от условного bounding box — раньше это могло давать смещённые/неверные накопительные размеры на скошенных краях.
- Убраны условные подписи V1/V2/V3.../H1/H2/H3... для Shape — теперь просто накопительные размеры, единообразно с прямоугольником.
- Для прямоугольных деталей поведение из коммита 1 сохранено без изменений (в коде явно помечено комментарием «согласованная логика — не меняем»).
- Есть вспомогательные функции подгонки шрифта под доступное место (`fitText`) — на плотных чертежах текст автоматически уменьшается, чтобы не наезжать на соседние размеры.

**Как заливалось:** оба раза — через веб-загрузку файла (`Upload files`, drag-and-drop) в GitHub, а не редактированием в браузере (см. раздел 3, «Workflow разработки»). Заливка проверена побайтовым `diff` между тем, что отдал пользователь, и содержимым на GitHub после коммита — оба раза совпадение полное.

**Не проверено отдельно:** визуально на реальных нестандартных Shape (трапеция/скошенные углы) через сам интерфейс — проверка была только через regression-тесты (`test/run.js`, эталонные числа) и байтовое сравнение файлов. Если пользователь увидит визуальные огрехи на конкретной фигуре — заводить как новую правку, не считать этот аудит финальным.

### 9б. Повторный аудит и глубокая архитектура (18 авг 2026)

Повторно проверены исходники и собранный файл. Исправлены воспроизведённые дефекты безопасности, валидации Shape/Muntin, импорта/localStorage, нормализации навыков, RU/EN, мобильной оболочки и CI. Эталонные числа геометрии v4.5 не менялись. Набор проверок расширен с 38 до **57**; подробный список — `docs/REVIEW_2026-08-18.md`.

Отдельно создан `docs/DEEP_ERP_ARCHITECTURE.md`: три варианта платформы, рекомендуемый модульный монолит, component genealogy, inventory/WMS, QMS, APS/MES, offline PWA, edge-agent, outbox, security, backup/observability, этапы и список недостающих производственных данных.

Пользователь одобрил расширенный целевой контур «цифровой завод»: **CPQ, MES/APS, прослеживаемость листа и обрези, NCR/remake genealogy, maintenance, energy, клиентский/поставщицкий портал, BI/прогнозирование и мультизавод**. Считать это целевой картой проекта. Конкретные процессы, stack option и приоритеты всё ещё утверждаются отдельно; не выдавать предложения документа за уже собранные требования производства.

### 9в. Перенос производственной визуализации Shape из Designer-версии + печать/PDF (20 авг 2026)

Пользователь передал ещё один файл внешней разработки — `GLASS_ERP_DESIGNER_CODEX_FIXED.html` (и следом `GLASS_ERP_PRODUCTION_SHAPES_GITHUB_READY.zip`) — с явно более качественной визуализацией Shape, и явно попросил перенести именно визуальный слой и часть UI-механики в модуль на GitHub, **не трогая геометрию v4.5**. Работа выполнена как отдельный, тщательно протестированный перенос, а не переписывание Shape с нуля.

**Перенесено:**
- Новый модуль `src/modules/shape/annotate.js` — производственный слой аннотаций (цепочки размеров, стрелки, подписи скосов, Overhead View для митры), портированный из Designer-файла. Подключён в `build/manifest.json` и `src/index.html` **до** `drawing.js`.
- **Управление и создание Smart-Shape через матрицу Edge** — заменяет прежний раздел «Out of plumb / level». Матрица показывает A/B/C/D одним взглядом, с цветовой идентичностью рёбер (`SHAPE_EDGE_HEX`: A `#2828dc`, B `#28b428`, C `#fe8d28`, D `#a00082`), едиными для чертежа и для UI.
- **Скошенные рёбра нотча (corner blocks) получили полноценное управление и визуализацию.** До переноса добавить скос ребру нотча в интерфейсе GitHub-версии было нельзя вообще — только в новом Designer-интерфейсе. Перенесены: матрица нотча, поля направления/величины скоса, отображение скоса внутри контура фигуры (раньше показывался только на прямых рёбрах A/B/C, скосы нотча визуально терялись).
- **Митра Front/Back и фацет (bevel) получили сторону обработки как производственный параметр**, а не просто оформление: Front Mitre оставляет длинной лицевую грань и подрезает тыльную, Back Mitre — наоборот. От этого зависит вид сверху (Overhead View) и тип машинной линии на чертеже.
- Чертёж (Production Drawing / Cutting Shape) визуально приведён к стилю Designer-файла: контур в цвет рёбер, цепочки размеров со стрелками, лист адаптивного размера под пропорции детали.

**Пять багов, найденных пользователем при ревью, и их причины:**
1. **`1″` отображался как `57/64″`, `10″` кромка нотча — как `10-7/64″`.** Причина: `ssStairs` анкерил лесенку нотча от вершины bounding box, а не от реального конца обрезанного пролёта, из-за чего первое ребро нотча забирало на себя наклон ребра A. Исправлено анкеровкой от `pVert` с выводом горизонтальных отрезков из фактических концов вертикальных.
2. **Скосы нотча не показывались внутри контура фигуры**, хотя прежняя (не GitHub) визуализация их рисовала. Причина — старая цепочка размеров не включала кросс-осевые рёбра нотча. Добавлена `shapeAnnChainSegments` с явным подбором рёбер по стороне.
3. **Левая мини-панель писала «невалидна», правая панель — «валиден» для той же фигуры**, и смена фигуры туда-обратно «чинила» левую. Причина — `refreshShapeEditor()` обновлял только правую колонку DOM. Исправлено точечным обновлением обеих панелей с сохранением фокуса поля ввода.
4. **Двойной скос становился невалидным сразу после ввода**, но валидным после переключения на другую фигуру и обратно — тот же класс бага, что и №3.
5. **Раздел «Corner blocks · Out of plumb / level» показывался, даже когда ни один угол не выбран.** Исправлено: секция видна только когда выбран хотя бы один угловой блок (`shapeCornerSectionsVisible`).

**Скрытая геометрия из снятых угловых блоков (отдельный, более глубокий баг).** После первого исправления пользователь показал точное воспроизведение: фигура `25″ × 92″` продолжала хранить невидимый вынос угла TL `2″`, хотя угловой блок в интерфейсе был снят. Первая правка в `ssNormalize` не сработала для уже сохранённых в localStorage фигур, потому что `ssIsModel()` делает ранний выход для «уже нормализованных» моделей и не перепроверяет их повторно. Исправление перенесено в сам `ssIsModel()`: модель без выбранных угловых блоков считается нормализованной только тогда, когда её `cornerOffsets` пусты — иначе она обязана пройти повторную нормализацию, которая обнулит осиротевшие координаты. Проверено прогоном с намеренно испорченной фигурой, засеянной прямо в `localStorage`.

**Нейтральный старт Smart-Shape.** По прямому требованию пользователя («пусть смарт шейп изначально не хранит никакую фигуру, максимум прямоугольник, но без скосов и ничего прочего») новый Smart-Shape открывается прямоугольником `1″ × 1″` без единого скоса, элбоу или углового выноса. Ограничено типом `smart` — остальные пресеты (circle, trapezoid и т.д.) сохраняют собственные дефолты, иначе рождались невалидными.

**UX по пустым полям.** Пустое числовое поле теперь автоматически считается нулём при расчёте, а не блокирует валидацию молча — вместе с этим поля, требующие ввода, подсвечиваются (`shapeMarkFields`, класс `input.bad`), чтобы не искать нулевое значение вручную по всей форме.

**Размер чертежа.** По жалобе «слишком маленький», затем «не влезает, приходится скроллить» — добавлена адаптивная подгонка листа под доступную высоту экрана (`shapeFitPreview`), домен-хром прячется на время редактирования (см. также правку в `sales.js`), правая колонка расширена.

**Печать на бумагу / сохранение в PDF.** Кнопка «Печать / PDF» добавлена в Shape (рядом с вкладками Setup / Production Drawing / Cutting Shape) и в Muntin (над чертежом). Реализация без всплывающих окон — печатный лист собирается в отдельном скрытом хосте (`src/erp/storage.js`: `printSheetHost`, `printSheetPrepare`, `printSheet`, `printSheetCleanup`) и показывается только через `@media print` + класс `body.printing`, поэтому работает и при открытии файла с диска (`file://`). Отдельно исправлена пропажа стрелок размеров в PDF: `url(#shpArr)` в SVG резолвится в первый совпавший `id` документа, и печатная копия «одалживала» маркер у скрытого превью — маркеры в печатной копии теперь получают уникальный суффикс (`printSheetUniqueIds`).

**Тесты.** Набор регрессий вырос с 57 до **82**: закрытость контура при скосе нотча и его площадь, валидация скоса, анкеровка нотча и сумма цепочки размеров, орфанные `cornerOffsets` (и для свежей модели, и для уже сохранённой), нейтральный старт 1×1, самодостаточность печатного листа и наличие ссылок на маркеры размеров в нём. Всё проверялось Playwright + реальный Chromium (`node test/run.js` для `src`, `TARGET=dist node test/run.js` для собранного файла) — не Node `vm`, потому что `ShapeModule` объявлен через `const` и в `vm`-контексте не всплывает наружу.

**Не перенесено, обсуждалось с пользователем, ждёт отдельной задачи:** составные изломы (элбоу) на рёбрах нотча; центральный верхний нотч (MID_TOP) из Designer-файла; разделение SDL front/back на две отдельные перемычки в BOM Muntin.

### 9г. Независимый аудит после merge Stage 3B (21 авг 2026)

Проверка выполнена отдельной сессией Claude Code на коммите `29b5fea` — не по документам, а на живой системе: клон репозитория, локальная пересборка, прогон тестов, ручной проход по интерфейсу в браузере. Полный отчёт и план — `docs/PLAN_2026-08-21.md`.

**Подтверждено рабочим:**
- `node build/check-manifest.js` — 41 модуль подключён и в сборке, и в dev-режиме.
- `node build/build.js` — сборка воспроизводимая; `dist/GLASS_ERP.html` в репозитории совпадает с локальной сборкой из `src` (разница только в переносах строк Windows). **Живая версия актуальна.**
- `node test/run.js` — **108 passed, 0 failed**; `TARGET=dist node test/run.js` — тоже 108/108.
- GitHub Actions на последнем push в `main` — success. Ошибок в консоли браузера нет ни на одном экране.
- **Сквозной сценарий продажи прошёл целиком:** новый клиент `C-00001` → Sales Order `76002` → Makeup A (Double) → строка 48 × 36 сохранилась как `width16: 768`, `height16: 576` → `+ Shape` открыл конфигуратор с уже подставленными именем и размерами → «Сохранить ревизию» вернуло в Sales с `shapeRef · revision 1 · fingerprint shp-d305869b` (12.00 ft², периметр 168″) → `+ Muntin` открыл раскладку, привязанную к этому Shape (3 сегмента, total cut 117 3/8″, сетка 1/16″, отступ 7/16″) → Save Draft → F5 → всё на месте.
- Аккордеоны Lite/Cavity стартуют закрытыми и держат одну открытую секцию — проверено в DOM.
- Мобильные 375 px: горизонтального переполнения нет нигде, включая редактор Sales Order.
- Тем самым закрыты пункты 1–4 и 6–8 merge-gate из `docs/STAGE3B_FINAL_AUDIT.md`. **Не проверен только пункт 5** — что тёмная левая колонка продолжается до низа на длинном заказе; скриншот в той среде снять не удалось, нужен взгляд глазами.

**Найдено — восемь ловушек в интерфейсе (Этап 3C плана):**

| # | Дефект | Где | Риск |
|---|---|---|---|
| B1 | Кнопка Close стирает черновик заказа без подтверждения | `sales/orders.js:22` `salesOrderClose()` | Оператор ввёл 50 строк, промахнулся мимо Save Draft — всё исчезло молча |
| B2 | F5 убивает несохранённый заказ | `soDraft` живёт только в переменной | То же самое по случайному обновлению страницы |
| B3 | Заказ сохраняется со строками без размеров | `sales/orders.js:23` `salesOrderSave()` проверяет только `customerId` | Пустые строки уезжают в Draft, ошибка всплывёт в цеху |
| B4 | Excel-вставка ждёт `Qty \| Height \| Width`, а таблица показывает `Width \| Height` | `sales/orders.js:69` `salesExcelApply()` | Перепутанные ширина/высота на целой пачке строк |
| B5 | «● несохранённые изменения» горит всегда | флаг `dirty`, `storage.js:13` и `:20` — сбрасывается только экспортом JSON | Оператор либо паникует зря, либо привыкает и пропустит настоящее предупреждение |
| B6 | Модуль Sales только по-английски даже в RU | словарь `i18n.js` односторонний RU→EN, строки Stage 3B в нём отсутствуют | Половина системы русская, половина английская |
| B7 | `docs/STAGE3B_FINAL_AUDIT.md` п.8 обещает CI на feature-ветках и PR, в `build.yml` этого нет | только `push: branches: [main]` | Можно забыть ручной запуск и влить непроверенное |
| B8 | Пользователей ноль на старте | `DB.user` пустой | Дашборд показывает 0, отчёт по навыкам пустой |

**Найдено — шесть блокеров реального использования** (это не баги, а отсутствующие куски системы): цены нет вообще; разузлования строки в компоненты нет; журнала событий нет; заказ упирается в единственный статус `draft` (`sales/data.js:12`); экрана справочников нет — каталог 16 позиций стекла, 15 из них 6 мм, правится только через JSON; всё в localStorage на одного человека. Первые три — критические, порядок работ по ним в плане.

**Вывод аудита:** инженерное ядро крепкое, ловушки чинятся за полдня. Дальше идёт ядро — журнал событий, цена, компоненты, — а не новые экраны.

---

## 10. Где что лежит (состояние на 21 августа 2026)

### Единственный источник правды по коду — репозиторий `MaksymPrudnikov/ERP`

```
index.html                    точка входа GitHub Pages → открывает собранную систему
.nojekyll                     без него Pages подменяет index.html отрендеренным README

dist/GLASS_ERP.html           СОБРАННАЯ СИСТЕМА. Двойной клик, работает без сервера.
                              Пересобирается роботом. Руками не редактировать.

src/index.html                та же система, модули подключены отдельными <script src>.
                              Правишь модуль → F5. Живёт на /ERP/src/
src/shell.html                каркас страницы (шапка, меню, контейнер)
src/styles/                   base.css · modules.css

src/core/                     dim.js (разбор дюймов) · poly.js (площадь, габарит, самопересечение)
src/modules/shape/            consts · model · contour · presets · schema · features · validate ·
                              geometry · cutting · annotate · drawing · index
src/modules/muntin/           catalog · model · layout · clip · adaptive · bom · drawing · index
src/erp/                      data · icons · i18n · nav · storage
src/erp/customers/            data.js — Customer Master
src/erp/masterdata/           glass.js — seed каталог стекла/spacer/газа/герметиков/interlayer
src/erp/sales/                data.js (схема SalesOrder · OrderMakeup · OrderLine) ·
                              orders.js (черновик, Excel, мосты в Shape/Muntin) ·
                              makeup-ui.js (Makeup Builder)
src/erp/views/                dashboard · users · customers · sales · sales-shape-ui ·
                              sales-muntin-ui · configurators · optimization · production

build/manifest.json           ПОРЯДОК подключения модулей — новый файл вписывать сюда
build/build.js                собирает dist/GLASS_ERP.html из модулей
build/check-manifest.js       ловит файл, который добавили в src/, но не подключили

test/run.js                   108 регрессионных тестов: эталонные числа геометрии v4.5,
                              устойчивость оболочки к битым данным, Customers,
                              Sales Orders / Makeups, мост Shape↔Muntin, RU/EN, mobile

.github/workflows/build.yml   АВТОМАТИКА: после каждой правки в src/ проверяет манифест,
                              пересобирает dist, гоняет тесты, коммитит результат.
                              Тесты упали → dist НЕ обновляется, живая версия цела.

docs/GLASS_ERP_HANDOFF.md     этот документ
docs/PLAN_2026-08-21.md       текущий план: этапы 3C…9, критерии готовности,
                              список данных, которые нужны от пользователя
docs/REVIEW_2026-08-16.md     разбор 7 багов, найденных при ревью кандидата
docs/REVIEW_2026-08-18.md     повторный аудит: security, validation, storage, mobile, CI
docs/STAGE3B_SALES_MAKEUPS.md как устроены order-scoped Makeups
docs/STAGE3B_FINAL_AUDIT.md   предзаливочный аудит Stage 3B + merge-gate чек-лист
docs/STAGE3B_TEST_REPORT.md   что проверялось локально перед заливкой
docs/DEEP_ERP_ARCHITECTURE.md целевая модель глубокой ERP и вариант «цифровой завод»
docs/ДЛЯ_ЧАТГПТ.md            что можно и нельзя менять — отдавать внешнему ИИ
```

### Что реализовано в системе

- **Dashboard** — визуальная карта ERP по доменам, KPI, дорожная карта, список того, что требует решения пользователя.
- **Users** (ядро) — сотрудники, привязка к станции, производственные навыки с уровнем (Новичок / Мидл / Синьор), отдельный отчёт по покрытию навыков. Слово «навык» здесь = компетенция сотрудника, к программным модулям отношения не имеет.
- **Customers** — Customer Master: код клиента, контакты, адреса, кредитные условия, `legacyRefs` под перенос из Spil, CSV/JSON импорт и экспорт, merge по Account без дублей, защита от удаления клиента со ссылкой из Sales Order.
- **Sales** — Draft Sales Orders: авто-номер, шапка (клиент, PO, срок, приоритет, доставка, условия, валюта, branch), order-scoped Makeups A/B/C с Makeup Builder, строки заказа с размерами в 1/16″, массовая вставка из Excel, назначение Makeup выделенным строкам, кнопки `+ Shape` / `+ Muntin` на строке. Подробности — раздел «Stage 3B закрыт» в начале документа.
- **Configurators** — два кликабельных модуля-карточки: **Smart-Shape / Advanced** (A/B/C/D через матрицу Edge, скошенные рёбра нотча со своей визуализацией, corner blocks, митра Front/Back и фацет как производственная сторона, Overhead View, валидация, реальный контур, нейтральный старт 1×1, печать/PDF — см. раздел 9в) и **Adaptive Muntin** (equal-clear на сетке 1/16″, обрезка реальным периметром, перпендикулярная привязка к кромке, отдельные cut lengths на каждый сегмент бара, производственный чертёж с обновлённой системой размеров, печать/PDF — см. разделы 9а и 9в, BOM).
- **Optimization** — визуальный мост ERP → локальный bridge → Perfect Cut. Кнопка отправки батча намеренно неактивна: протокол не подтверждён.
- **Production** — поток по уровням, карточки станков, точная CRUD-таблица. CNC1 и FURN1 без уровня — ждут решения пользователя.
- **Inventory / Purchasing / Shipping / Finance** — видны в меню как план, экранов нет.
- **RU / EN** — переключается в шапке, выбор сохраняется. Переводится интерфейс; данные, введённые пользователем, не переводятся.
- Экспорт/импорт JSON и автосохранение в localStorage.

### Историческое (не путать с актуальным)

- `GLASS_ERP_CLAUDE_REVIEW_CANDIDATE.html` — снимок ДО ревью, из него вырос репозиторий.
- `glazing_system_v1.html` (и визуальные v2/v3) — ранняя доменная оболочка с заглушками Shape/Muntin.
- `glass_erp_materials_v1/v2/v3.html` — прототипы модели материалов и цены. **`v3` остаётся справочным источником по разделам 4–5**, пока эти экраны не перенесены в новую оболочку.
- `GLASS_ERP_PLAN_v1.md` — исходный план 5 фаз (раздел 8 — актуализированная версия).

### Не в репозитории, нужно донести

- `schema/01_substrate.sql` … `05_service.sql` — пять миграций, отражающих модель материалов из раздела 4 (включая функции `check_treatment_fits()` и `check_route_fits()`).
- `Типы_стекла_шаблон.xlsx` — таблица для ручного заполнения типов стекла.

---

## 11. Открытые вопросы / что нужно от пользователя дальше

**Нужны данные от производства:**

Статусы на 21 августа 2026 после ответов пользователя.

1. ~~**Печь:** минимальный размер детали~~ — **закрыто: 6 × 12″.** Максимальный вес садки — **закрыто оценкой, не паспортом**: ≈ 305 кг, выведено из «15 мм стекла 90 × 140″» (раздел 4.3). Сверить с документацией печи до планирования садок.
2. ~~**Уровень потока для CNC1**~~ — **закрыто: ЧПУ работает на двух уровнях.** Shape polishing — уровень 2, processing (notch/radius) — уровень 3, см. раздел 4.2. **`FURN1` остаётся открытым** — пользователь не сказал, куда именно встраивается закалка в маршрут. Не угадывать: от этого зависит, что считается pre-temper операцией.
3. **Полный список станков — ОТКРЫТ.** На вопрос пользователь ответил «144×96 думаю но не утверждаю» — это **размер листа стекла, а не станок**. Значение полезно и совпадает с листами Vitro/Cardinal из раздела 4.3, но список рабочих центров по-прежнему не получен. Нужны мойка, линия IGU, автоклав, ламинация и остальные, с габаритами и уровнями.
4. ~~**В каких толщинах реально доступно каждое покрытие**~~ — **в работе:** пользователь готовит каталог в Excel и просил помочь превратить его в базу. Шаблон колонок и правила заполнения — в `docs/PLAN_2026-08-21.md`. **Главное правило заполнения:** одна строка = один реально закупаемый продукт в конкретной толщине; **закалку в код продукта не включать** — `TEMP`/`HS` это обработка, а не материал (раздел 4.1).
5. ~~**Реальные цены закупки**~~ — **закрыто решением:** временно 1.00 за ft² по всем позициям, реальные цены пользователь впишет позже (раздел 5.3).
6. ~~**Курс USD→CAD**~~ — **закрыто: 1.37 подтверждён.**
7. ~~**Роли пользователей**~~ — **закрыто: Продажи · Бухгалтер · Админ · Владелец.** Прибыль видит только Владелец. Подробности и следствия — раздел 7.
8. **Механизм обмена Perfect Cut ↔ Spil — ОТЛОЖЕН пользователем** («давай позже», 21 авг 2026). Формат моста по-прежнему не проектировать. Напоминание: ответ от R.O. SRL идёт долго, письмо стоит отправить раньше, чем он понадобится.
9. **Эталонные заказы Spil — ОТЛОЖЕНЫ пользователем** («давай позже»). Ценообразование строить можно, но **проверенным оно не станет**, пока не с чем сверить: критерий выхода Ф2 — «расхождений по цене и составу деталей нет».
10. ~~**Energy surcharge: до налога или после?**~~ — **закрыто: до налога, 8.75 %.** Порядок расчёта из раздела 5.2 остаётся без изменений.

**Технические решения, ожидающие ответа:**

11. **Журнала событий нет.** Раздел 6 требует писать событие на каждое действие в каждом модуле. В прототипе не реализовано — это отдельный слой ядра, не правка экрана. Чем позже начать, тем больше истории потеряно безвозвратно.
12. **Донести SQL-миграции и xlsx-шаблон** в репозиторий (см. раздел 10).
13. **Визуальное направление.** Пользователь одобрил общий рост визуальности, но оценил как «лучше, но не идеально ни визуально, ни логически». Следующая итерация — по его конкретным замечаниям, без самостоятельного большого редизайна.
14. **Входные данные глубокой ERP.** Пройти список из раздела 15 `docs/DEEP_ERP_ARCHITECTURE.md`: product families, реальные маршруты/ограничения, lots/offcuts, pricing, quality, labels, offline/SLA и approval matrix.
15. **Восемь ловушек интерфейса** из аудита 21 августа (B1–B8, раздел 9г) — Этап 3C плана, примерно полдня работы. Самая опасная — B4: Excel-вставка ждёт размеры в другом порядке, чем показывает таблица.
16. **Merge-gate п.5 не проверен глазами** — тёмная левая колонка на длинном Sales Order. Всё остальное из чек-листа `docs/STAGE3B_FINAL_AUDIT.md` подтверждено.
17. **Заказан экран работы с базой** (пользователь, 21 авг 2026): посмотреть все данные системы своими руками и сказать, чего в модели не хватает и что лишнее. Хранение остаётся браузерным. Это ближайшая работа по коду — см. `docs/PLAN_2026-08-21.md`.

**Закрыто:**

- ~~Stage 3B: Draft Sales Orders с order-scoped Makeups~~ — закрыто 21 авг 2026, PR #3 влит в `main`, CI зелёный, живая версия обновлена, независимый аудит подтвердил (раздел 9г).
- ~~Решение по глобальной библиотеке конфигураций~~ — закрыто 21 авг 2026: отвергнута по комбинаторике, взамен order-scoped Makeup + вычисляемый fingerprint (разделы 2 и 5.4).
- ~~Когда переезжать на сервер~~ — закрыто 21 авг 2026: пока не переезжаем, хранение браузерное, вместо переезда строится экран работы с базой для приёмки модели пользователем (раздел 3).
- ~~Параметры расчёта: курс, минимум ft², отход, energy surcharge, placeholder цены~~ — закрыто 21 авг 2026, раздел 5.5.
- ~~Роли пользователей и кто видит прибыль~~ — закрыто 21 авг 2026, раздел 7.

- ~~GitHub: репозиторий и токен~~ — закрыто 16 авг 2026. Репозиторий создан, код залит, Pages включён, автосборка настроена.
- ~~Ревью кандидата перед заливкой~~ — проведено, 7 багов исправлено, см. `docs/REVIEW_2026-08-16.md`.
- ~~Обновление чертежа Muntin (production setout + аудит геометрии Shape)~~ — закрыто 17 авг 2026, см. раздел 9а. Оба коммита прошли тесты и залиты.
- ~~Перевод ошибок Shape/Muntin в оболочке~~ — закрыто 18 авг 2026: модули возвращают нейтральные reason/code, RU переводится в `erp/i18n.js`.
- ~~Мобильный document overflow~~ — закрыто 18 авг 2026 для текущих экранов, проверяется viewport 390 × 844. Реальные устройства и плотные производственные чертежи всё равно требуют визуального acceptance.
- ~~Повторный аудит текущего прототипа~~ — закрыто локально 18 авг 2026, 57/57 regression tests; см. `docs/REVIEW_2026-08-18.md`.
- ~~Перенос производственной визуализации Shape из Designer-версии, скосы нотча, матрица Edge, митра Front/Back, печать/PDF~~ — закрыто 20 авг 2026 в коде и тестах (82/82), см. раздел 9в. Заливка на GitHub через `Upload files` — вручную пользователем, т.к. `git push` из облачной сессии заблокирован прокси (см. ниже); если этот пункт читается позже своей даты и живая версия ещё не показывает матрицу Edge — заливка не завершена, проверить `https://maksymprudnikov.github.io/ERP/dist/GLASS_ERP.html`.

---

## 12. Инструкция для продолжения (для ИИ, который подхватит проект)

**Про код и репозиторий**

- Код живёт в `MaksymPrudnikov/ERP`, разрезан на 34 модуля. **Не присылать пользователю новый большой HTML «взамен»** — правится ОДИН модуль в `src/**`, дальше робот сам пересоберёт `dist`. Пересборка файла целиком руками — ровно то, от чего пользователь отказался.
- **Перед правкой геометрии прогнать `node test/run.js`, после правки прогнать снова.** Эталонные числа там не для красоты: трапеция 48 × 36 при C = 30 обязана давать бары `33 7/64″ · 31 1/8″ · 47 1/8″`. Разошлось — сломан перенос v4.5, а не тест. На 21 августа 2026 набор даёт **108 passed, 0 failed** — и на `src/`, и на собранном `dist`.
- **Порядок работ брать из `docs/PLAN_2026-08-21.md`.** Он не «пожелания», а следствие аудита: сначала журнал событий (единственное, что теряется безвозвратно), потом справочники, цена, компоненты, жизненный цикл заказа. Не начинать новые экраны раньше ядра.
- **Добавил файл в модуль — впиши его в `build/manifest.json` и `src/index.html`**, в одинаковом порядке. Иначе `check-manifest` остановит сборку.
- **Исправления известных особенностей v4.5 — отдельным коммитом**, не смешивая с изменением интерфейса, чтобы можно было откатить одно, не потеряв другое.
- **Прямой `git push` из облачной сессии Claude (Cowork) не проходит** — прокси пускает только к репозиториям, добавленным в «sources» задачи. Либо добавить репозиторий в источники при старте задачи, либо заливать через `Upload files` на GitHub (не через встроенный редактор — см. раздел 3). `device_bash` на машине пользователя работает без сети, git оттуда невозможен. В Claude Code (CLI) этого ограничения нет — push идёт напрямую.

**Про архитектуру — не переизобретать**

- **Не возвращать глобальную библиотеку конфигураций** — раздел 2 и 5.4. Комбинаторика: 331 776 сочетаний одного только Double на текущем каталоге, где пока есть лишь 6 мм. Makeup живёт внутри заказа; общая личность одинаковых пирогов — через вычисляемый fingerprint, не через хранимый каталог.
- Не предлагать миграцию базы Spil: решение строить с нуля финальное.
- Не проектировать свой оптимизатор раскроя — интеграция с Perfect Cut (раздел 3).
- Не изобретать формат обмена с Perfect Cut, пока не пришли реальные настройки коннектора.
- Уважать разделение осей из раздела 4: подложка / покрытие / толщина / обработка, тройное состояние `temper_mode`, `exposure_rule`.
- Новый модуль — сверяться с картой доменов (раздел 6), не мешать сущности разных доменов в один экран.
- Права доступа — через единую точку проверки (раздел 7), не размазывать по экранам, проверять на выдаче данных, а не в интерфейсе.
- Каждое действие в любом будущем модуле обязано писать событие в журнал (раздел 6).

**Про модули Shape и Muntin**

- Перенесены из Glass Configurator v4.5, **не переписываются с нуля**. Не «улучшать» геометрию попутно без отдельного тестового шага.
- **Не добавлять размеры стекла в Muntinbar.** Width/Height и контур принадлежат Shape; Muntinbar получает выбранный Shape по ссылке. Один источник геометрии — намеренно. Проверяется тестом.
- Модуль не знает про цены, клиентов и заказы. Эта развязка сделана специально.
- Слово «модуль» = обычный файл с кодом, **не** формат Claude Skills. Не путать.
- **Чертёж (`drawing.js`) обновлён 17 авг 2026** — прямоугольные и Shape-детали используют разные, но согласованные схемы простановки размеров, см. раздел 9а перед тем как трогать этот файл снова.
- **Shape получил слой аннотаций `annotate.js` и матрицу Edge 20 авг 2026** (перенос из Designer-версии, скосы нотча, митра Front/Back, печать/PDF) — см. раздел 9в перед правкой `drawing.js`, `annotate.js`, `model.js`, `contour.js`, `schema.js`, `validate.js` или `sales-shape-ui.js`. Инвариант «нет углового блока — нет `cornerOffsets`» защищён в `ssIsModel()`, не убирать эту проверку без замены.

**Про интерфейс**

- Shape и Muntinbar показывать отдельными иконками-карточками, не текстовым списком. Навыки сотрудников — тоже иконками, где это помогает читать.
- **RU/EN переводит интерфейс, а не данные.** Значения из базы выводить через `raw()`; `esc()` для пользовательских значений больше не использовать, иначе переводчик снова начнёт менять содержимое базы.
- **Мобильная оболочка адаптирована 18 авг 2026** и имеет regression test на 390 px. При новых широких таблицах/редакторах сохранять локальный scroll и добавлять мобильную проверку; SVG-геометрию ради CSS не менять.

**Про общение**

- Стиль — русский/украинский смешанный, конкретно, без лишней теории, всегда с примерами на реальных данных пользователя (Vitro, Cardinal, конкретные толщины и станки).
- Пользователь не программист и учится через визуальную итерацию. Сырой SQL как единственный артефакт не годится — всегда рабочий экран.
- При обновлении этого документа — **точечные правки в нужный раздел, не переписывать файл целиком.** Так прямо просил пользователь.
