# ERP для производства стекла — хендофф-документ

Версия 5.64 · 21 сентября 2026 — документы подчищены: архив вынесен, снимки переделанных экранов убраны, хендофф разрезан.


## Сделано: чистка документов · 21 сентября 2026

Владелец: «можешь подчистить ненужные документы в GitHub?» — и на список:
«удали всё, что не нужно в использовании».

Было 106 файлов и 13 МБ, стало 58 и 6,9 МБ.

**Убрано.** Десять файлов, которые КАРТА-ПРОЕКТА сама помечала архивом («читать
можно, следовать нельзя»): два ревью кандидатов, которых давно нет, отчёт о
прогоне 21 августа, стартовый контекст чата 27 августа, три выполненных
задания, заглушка вместо дубля плана и корневой ЧИТАТЬ.md. Раздел «Документы:
архив» из карты ушёл вместе с ними.

Четыре папки снимков на 6 МБ — экраны, переделанные с тех пор: раскрой до
линий реза и контраста, батчи, PR-B, компактный Sales. Они не просто лишние,
они врали. Снимки стикеров и NCR остались — те экраны живые.

Три обзора PR (`PR_B_REVIEW`, `SALES_COMPACT_REVIEW`, `GLASS_BATCHES_REVIEW`)
были галереями на 7–11 снимков каждая; без снимков от них остались пустые
оболочки, поэтому ушли следом. Ссылки на всё удалённое вычищены из живых
документов.

**Хендофф разрезан.** Он дорос до 6498 строк, и новая сессия тратила половину
чтения на то, чего уже нет на экране. В основном файле остались версии от 5.36
(17 сентября, рождение своего раскроя) и новее — 1022 строки; всё старше ушло
в `docs/HANDOFF_АРХИВ.md` (5494 строки) со ссылкой из README и карты.

**Проверка документов.** За этот день скрипт правки документов дважды разрезал
строку версии пополам, и ни одна проверка этого не увидела — документы они не
читали. Теперь `test/docs.js` сверяет версию в хендоффе и в карте, целость
строки версии и то, что живые документы не ссылаются на удалённые файлы, а
`test/run.js` в конце сверяет счёт проверок в README с настоящим. Ошибка в
самой проверке нашлась сразу: регулярка искала «проверк», а в тексте
«проверок» — другая форма слова, и счёт молча читался нулём.

`test/docs.js` — 3 проверки, полный набор 807.

## Добавлено: список, приоритеты и размеры листов · 21 сентября 2026

Три замечания владельца с настоящего батча.

**Снятая последняя галочка запирала экран.** «Убираю последнюю галочку — меня
выбрасывает и не даёт пересобрать, пока не создам новый размер, а я просто
отовсюду убрал галочку и хочу включить в другом месте». `cutPlanDraft`
пропускал стекло целиком, если `cutStockFor` вернул пусто, — группы не
появлялось, экран схлопывался в жёлтую строку «No sheet size» с одним полем
«Add and build», а таблица размеров исчезала вместе с галочками. Поставить их
обратно было негде. Теперь группа создаётся, пока в цехе известен хотя бы один
размер: таблица остаётся, галочку можно вернуть, а Build по-прежнему откажется,
пока размер не отмечен, — про это и говорит жёлтая строка.

**Лишние размеры теперь удаляются отсюда же.** «Удалить эту вакханалию
невозможно» — размеры цеха копятся от опечаток (400 × 200, 222 × 22), а убрать
их можно было только в Master Data → Cutting. У размера цеха в таблице SHEETS
появился ×: он спрашивает подтверждение и зовёт `cutShopSizeRemove`. Размер из
поставок стекла и размер, на котором уже лежат листы, не трогаем — у них
кнопки нет.

**Приоритеты больше не уводят экран.** «Когда нужно много стёкол дать
приоритет, неудобно: после смены одного приоритета всё скачет». Каждая правка
перерисовывает экран целиком, список уезжал в начало и поле теряло фокус.
`cutUiKeepSpot` запоминает прокрутку списка и поле, и возвращает их после
перерисовки — можно идти вниз по списку, не ловя строку заново.

**Столбики сортируются.** «Нельзя отфильтровать или отсортировать столбики».
Заголовки Glass ID, Cut size, Order, Customer, Pri и Sheet стали кнопками:
клик — по возрастанию, второй — по убыванию, третий — как было. Порядок живёт
в `cutUi.sort` и применяется ко всем трём спискам разом.

`test/cut-layout.js` — 62 проверки, полный набор 804.

## Предыдущее: формы в укладке и Safety border · 21 сентября 2026

Владелец: «я где-то прочитал, но потерял, что скосы не попадают в рез — я
ошибаюсь?» Не ошибся, но не про рез: скос в резе есть (заготовка растёт на
запас по косой стороне, а с 5.61 контур ещё и рисуется). Выбрасывало другое —
`cutPackColumns` начинался со строки `if(!stock.length||list.some(p=>p.shape))
return null;`: **одна фигурная деталь отменяла столбики и ряды для всего
батча**, то есть 12 кандидатов раскладки из четырнадцати. Замер на тесте
владельца: 152 стекла, одна форма вместо одного прямоугольника — 23 листа и
95,8 % превращались в 24 листа и 91,8 %.

Теперь столбики кладут формы: коробка формы раздувается на Min distance с
четырёх сторон, а стекло встаёт внутрь на тот же зазор — сосед любой стороной
оказывается не ближе Min distance, прямоугольники по-прежнему стоят вплотную.
Тест владельца с формой снова даёт 23 листа и 95,8 %.

**Попутно найден и починен старый баг полосной укладки.** В `cutPack`
вертикальный зазор между полосами спрашивали только у стекла, ОТКРЫВАЮЩЕГО
полосу (`cutEdgeGap(p,…)`). Если полоса открывалась прямоугольником, а в
полосе под ней лежала форма, прямоугольник садился вплотную: на случайных
батчах с формами это давало до 7 пар на батч по каждой стратегии — полоску
тоньше Min distance не сломать, а на экране это не видно. Теперь форма входит
в готовую полосу, только если сверху и снизу уже есть зазор, а новая полоса
отступает, если форма есть у неё самой или в полосе под ней. По горизонтали
зазор считается точно, как раньше, — прямоугольники ничего не потеряли.

Замер на 12 случайных батчах с формами (124 фигурных стекла из 756):
195 листов и 4 нарушения зазора → **194 листа и 0 нарушений** (193 без бордера,
плюс лист на сам бордер). Тесты
владельца и 20 случайных прямоугольных батчей — без изменений (23, 50,
433 листа), время Build прежнее.

**Safety border теперь занимает место на листе.** Владелец, глядя на белое
поле вокруг формы у Perfect Cut: «это бордеры, которые мы добавляем, я
почему-то не вижу в своём раскрое бордеры… ты что-то в оптимизацию взял не
всё». Так и было: `cutPieces` брал у лайта `cutW`/`cutH` — размер реза, — а
`footprint` (рез плюс Safety border) в раскрой не доезжал. В карточке формы
это видно тремя строками: Finished 20 1/8 × 91 3/16″, Cutting 20 5/8 ×
91 45/64″, Billable footprint 23 5/8 × 94 11/16″ (бордер 1 1/2″). Раскрой жил
по второй.

Теперь заготовка формы считается по `footprint.pad` — **по сторонам, а не
вокруг детали целиком**. Владелец на первом варианте сразу поймал лишнее: «у
тебя скосы только сверху — зачем расстояние между прямыми линиями слева и
справа?» Незачем: прямая сторона режется как у прямоугольника, вплотную.
Поэтому место откладывается только там, где `pad[сторона] > 0`, то есть куда
смотрит скос, и там берётся **большее из Safety border и Min distance, а не
сумма** (решение владельца: «возьмём первый вариант, если что поправим после
точной оптимизации и тестов Perfect Cut»).

Контур сдвигается внутрь заготовки на левый и нижний отступы. `pad` (самая
большая из отложенных сторон) едет со стеклом в раскладку, и `cutEdgeGap`
вычитает его из Min distance — сверх отложенного до соседа ничего не нужно.
Правки руками считают по тому же правилу.

На листе владельца это видно так: четыре формы со скосом сверху стоят
вплотную боками, белое поле только над скосами, а остаток вырос с
31 1/4 × 100 1/4″ до 33 1/2 × 100 1/4″.

Цена: +1 лист на 12 случайных батчах с формами (193 → 194). Это стекло клиент
и так оплачивает — строка Billable footprint.

Зазор вокруг формы в самих укладчиках остался запасным путём — на случай
формы без футпринта; проверка гоняет `cutPack` и `cutPackColumns` на таком
списке напрямую.

**Починена своя же регрессия скорости из 5.61.** `cutPieces` отдавал три новых
поля контура (`pts`, `holes`, `cutouts`) КАЖДОМУ стеклу, в том числе
прямоугольнику, где они были null. Укладчик копирует стекло на каждый из
сотен вариантов (`cutFillOrder` → `Object.assign({},p,{t0:p})`), и эти поля
стоили 60 % времени Build: тест 2 шёл 2,6 с вместо 1,65 с. Теперь контур
кладётся только форме и только когда есть что класть; проверка следит, что у
прямоугольника этих полей нет вовсе. Тест 1 — 0,83 с, тест 2 — 1,64 с, как до
5.61.

`test/cut-layout.js` — 60 проверок на тот момент.

## Предыдущее: форма на листе и размеры пустот · 21 сентября 2026

Владелец: «самый важный вопрос — это отображение шейпа и как он будет
резаться. У Perfect Cut видно форму, а у нас квадрат — а как же резать шейп и
скосы?» Прислал раскрой Perfect Cut с настоящим контуром, наш лист с тем же
стеклом прямоугольником 48 3/8 × 79 3/8″ и наш же чертёж этой трапеции.

**Геометрия у нас была вся, до листа она просто не доходила.** `cutPieces`
считает `salesEffectiveCuttingPlan(...)` и брал из лайта только `cutW`/`cutH`
— габарит заготовки. В том же лайте лежат `cuttingPoints` (контур, по которому
режут), зеркало уже применено, а `result.featureGeometry` даёт отверстия и
вырезы. Новая `cutShapeGeom(lite)` переводит их в координаты заготовки: ноль в
левом нижнем углу, ось Y вверх, как у листа; отверстия зеркалятся по той же
оси, что и контур. Габарит контура обязан совпасть с заготовкой — разошлись,
контур не отдаём, и стекло рисуется прямоугольником, как раньше.

`cutSheetSVG` рисует заготовку серым (это отход внутри неё), контур —
многоугольником со стеклянным градиентом, отверстия и вырезы — поверх.
Поворот `p.rot` — 90° **против часовой**: у несимметричной формы две стороны
поворота дают разные стёкла, и лист теперь показывает, какое из них. Подпись
уехала в центр тяжести контура: у трапеции середина заготовки попадает в
отход над скосом. Прямоугольные стёкла рисуются прежним `rect`. Печать идёт
тем же кодом — скос уезжает на стол.

**Размеры пустых мест.** «Вижу, где пустое пространство, но не знаю размеры —
вдруг я бы мог туда добавить… не хочу засорять экран, может больше 4 сквер
фит». `cutSheetCuts` уже отдаёт `free` — пустые листья дерева резов; вызов
поднят выше и переиспользуется и для пустот, и для линий. Каждый кусок
получает прозрачный `[data-cut-free]` с подсказкой по наведению, а подпись
`W × H″ · N ft²` — только от `CUT_FREE_LABEL_FT2` (4 ft²). Замер на батче
владельца (55 листов): 0,82 подписи на лист, максимум 3, у 40 листов из 55 —
ни одной. Порог 6 ft² оставил бы 15 подписей вместо 45; поднимать не
потребовалось.

Попутно `cutFlipCut` получил `cutPlanRefresh`: после 5.59 остатки считаются по
дереву резов, и перевёрнутый рез оставлял подсказки от старого дерева.

**Не сделано намеренно:** форма по-прежнему считается в Used целым
прямоугольником — треугольник над скосом попадает в «использовано». Считать
честно правильно, но Used на батчах с формами упадёт, а владелец сравнивает
цифры с Perfect Cut, поэтому это отдельный PR с числом «было / стало».
Раскрой «скос к скосу» — отдельная работа укладчика.

`test/cut-layout.js` — 57 проверок на тот момент.

## Предыдущее: отход одним куском · 20 сентября 2026

Владелец на своём батче (55 листов, два размера — 130 × 96 и 144 × 102,
раскладка столбиками): «почему ты оптимизируешь так, чтобы люди лишний раз
ломали кусок стекла по середине и не было возможности даже на маленький
остаток». На листе 8 × 34×36 и 4 × 24×24 стёкла 24″ стояли с шагом 34″ —
каждое в своей колонке, между ними полоски 10 × 24″.

Причина: `cutPackColumns` берёт ширину колонки по самому широкому стеклу в
ней, а узкое стекло оставляет полоску сбоку. Правка руками это чинила
(`cutCompactSheet` — стекло подъезжает к соседу), Build — нет. Это тот же
разрыв, что и с лишним листом: рука умнее автомата.

`cutTidyGroup` гоняет то же уплотнение после укладки — и на каждом варианте
раскладки, до `cutScore`, чтобы сравнение было честным. Лист, который после
уплотнения перестал бы резаться сквозными резами, откатывается;
заблокированные листы и закреплённые стёкла не трогаются.

Замер на батче владельца (55 листов, столбиками): лишних разломов посередине
было 45, стало 0; листов, ft² и Used — без изменений (стекла столько же).
На его листе 55 самый большой кусок отхода был полоской 6 1/4 × 100 1/4″,
стал 40 × 24″. Тесты владельца без изменений: 23 и 50 листов.

`test/cut-layout.js` — 55 проверок на тот момент.

## Предыдущее: лишний лист и честные остатки · 20 сентября 2026

Владелец принёс аудит раскроя, сделанный Codex по его просьбе, и попросил
проверить, что из него правда. Проверено своими замерами — 20 случайных
батчей на 144 × 102″, отступы 7/8 со всех сторон.

**Лишний лист.** Подтвердилось: укладчик «лист за листом» дробит свободное
место и обратно его не склеивает, поэтому последний лист держал стёкла,
которым на ранних листах место ещё было — ручной перенос это место находил,
а Build нет. `cutTightenGroup` после выбора лучшей раскладки пробует
растащить последний лист: его стёкла раскладываются вместе со стёклами
раннего листа тем же укладчиком `cutFillBest`. Тем же — потому что лист
обязан остаться резаемым сквозными резами: своё размещение «куда влезет»
тоже давало минус лист, но два листа из пяти после него не резались.
Заблокированные листы, закреплённые стёкла и листы с забуканным стоком не
трогаются; лист уходит, только если разошлись все его стёкла. Сначала
сравниваются площади: свободного места меньше, чем стекла на последнем листе
— дальше не считаем. Замер: минус 6 листов в 5 батчах из 20, Used
+1,8…+4,3 пункта, тесты владельца без изменений (144 × 102, Trim 1 — 23
листа; второй тест — 50), время Build прежнее (в среднем 0,57 с).

**Полоса отхода — целиком.** На снимке Perfect Cut верхняя полоса отхода
остаётся одним куском, а резы между стёклами заканчиваются на её границе. У
нас рез мог пройти сквозь отход и раскрошить его. Теперь `cutSheetCuts`
сначала отрезает пустую полосу, и первой — бо́льшую по площади; между
стёклами режет как раньше.

**Остатки считаются по резам.** Подсказка остатка была самым большим
свободным прямоугольником, а он не обязан совпадать с резами: на 12 батчах
46 подсказок из 176 (869 ft² из 3679, 24 % площади) рез разрезал надвое — на
такой кусок нельзя вешать номер стока и стикер. Теперь `cutFreeRects` берёт
пустые листья дерева резов: остаток — то, что снимется со стола одним куском.

**Линия первого реза** больше не почти чёрная и не толще прочих: владелец
про неё — «жёсткая чёрная полоса… она меня пугает». Все линии одного серого,
толщина 1,4 / 1 / 0,8 по ступени; клик по сквозному резу по-прежнему меняет
направление.

Из аудита не взято: запрет ручного поворота при выключенном авто-повороте
(в нынешнем виде это намеренно — вопрос владельцу), фигурная деталь в Used
по габаритному прямоугольнику, Undo/Redo, Web Worker.

`test/cut-layout.js` — 54 проверки на тот момент.

## Предыдущее: линии реза, контраст, высота · 20 сентября 2026

Владелец на настоящем батче: «у Perfect Cut есть направляющие полосы, как
лезвие будет резать полосами стекло, у тебя такой истории нет в интерфейсе —
значит при экспорте на стол у нас могут возникнуть проблемы»; «визуально нет
контраста цветов, где стекло, а где пустой лист… сделай лучше, в стиле
2026–2030, а не 1985»; «список и лист занимают много места по высоте,
уменьшил бы процентов на 20, чтобы таблица листов была видна на одном
экране».

**Линии реза** (`cutSheetCuts`, `cutSheetCutsFor` в `production/cut-layout.js`):
- Рекурсивная гильотинная разборка рабочего поля по стёклам и забуканному
  стоку (`cutTaken`): рез делит область надвое, каждая половина режется так
  же. Ступень 1 — рез через весь лист, 2 — внутри полосы, дальше мельче.
- Направление в каждой области — где общая длина резов меньше. Клик по
  сквозному резу переворачивает его (`cutFlipCut`, ключ области хранится в
  `sheet.flip`; повторный клик возвращает; если другого направления нет —
  «This cut can only go one way.»). Кликается только сквозной рез: тонкая
  линия у края стекла перехватывала бы захват мышью.
- Если в области больше одного стекла и сквозного реза нет (так бывает после
  правок руками), область обводится красным, в шапке листа — значок **Not
  cuttable** и кнопка **Re-pack sheet** (`cutSheetRepack` — складывает лист
  заново `cutFillBest`; заблокированный лист, закреплённое стекло и сток не
  трогает). Это и есть ответ на «при экспорте на стол могут быть проблемы»:
  теперь видно до стола.
- Линии идут и в печать — оператор режет по ним.

**Контраст**: заливки уехали из инлайна в классы и токены `:root`
(`cut-sheet-band`, `cut-sheet-free`, `cut-glass`, `cut-off`, `cut-stk`).
Стекло — холодный голубой с мягким бликом (`linearGradient #cutGlassSheen`,
стопы через CSS-классы `cut-g1…3`), пустое поле — ровный серый, поле обрезки
— темнее, углы стекла `rx=1.5`. Без штриховки и теней: на листе бывает 390
стёкол, и печать должна остаться чистой.

**Высота −20 %**: `<svg>` несёт `--cut-ar` (отношение сторон), CSS считает
ширину от высоты: `width:min(100%,calc(var(--cut-sheet-h,42vh)*var(--cut-ar)))`.
Рамка элемента совпадает с рисунком — мышь считает дюймы от неё. Плюс
уплотнение списка, шапки, ленты листов и полей. Замер на окне 1440 × 900:
лист 378 px (было ~520), нижняя граница таблицы SHEETS 888 px — видна без
прокрутки, остатки начинаются сразу под ней.

`test/cut-layout.js` — 52 проверки на тот момент.

## Предыдущая версия 5.57 · 20 сентября 2026 — «What if» парами: Trim X & Y и Border X & Y, стороны по отдельности — только у выигравшей пары.

## Изменено: «What if» парами · 20 сентября 2026

Владелец: «визуально оно разбито на 4 категории… сделай Trim X & Y и
Border X & Y, чтобы уменьшить нагрузку на глаза». Плюс замер, о котором он
спросил: на 20 батчах (его два теста, 12 случайных, 6 с круглыми размерами)
What if нашёл выигрыш в **2 случаях из 20**, зато крупный — −510 ft² (10 %) и
−102 ft² (4,5 %); оба раза это обрезка кромки. Дешёвая арифметическая
подсказка «здесь стоит нажать» проверена и отвергнута: 1 попадание,
1 пропуск, 3 ложные тревоги.

- Вместо четырёх строк — две: **Trim X & Y 0** и **Border X & Y 0** (пара
  показывается, только если хоть одна сторона больше нуля; одна сторона —
  строка так и называется).
- Если пара выигрывает, тут же считаются стороны по отдельности и идут под
  ней мелким шрифтом: «└ Trim X 0 only», «└ Trim Y 0 only» — вдруг хватит
  убрать одну. `Use` работает на любой строке.
- Обычный батч стал считаться вдвое быстрее: две прикидки вместо четырёх
  (замер: 111 мс против ~800 мс). Тест 2 владельца: пара даёт 42 листа
  (−816 ft²), под ней Trim X 0 — 46 листов, Trim Y 0 — 45.

`test/cut-layout.js` — 50 проверок, полный набор 794.

## Предыдущая версия 5.56 · 20 сентября 2026 — полезный остаток: 40 × 40 либо 10 ft², обе величины правятся.

## Изменено: полезный остаток — 40 × 40 либо 10 ft² · 20 сентября 2026

Владелец: «давай сделаем минимальный размер остатка, который у нас есть,
40 на 40 или мин 10 сквер фитов, точно так же чтобы можно было менять».
Причина: на его тесте 2 остатки — полосы 33 × 101″ (23 ft² каждая), а правило
«40 × 40» их не видело, и они уходили в мусор.

- `cutOffcutOk(w,h,params)` — кусок полезен, если **обе стороны** не меньше
  минимальных **либо** площадь не меньше `minOffcutFt2` (по умолчанию 10).
  Применяется и к подсказкам остатков (`cutFreeRect`), и к Split.
- `minOffcutFt2` — новая настройка рядом с Min offcut W/H: Master Data →
  Cutting (поле OR FT²) и на экране прогона (`or ft²`, правится и в собранном
  раскрое — подсказки не двигают стёкла). В `CUT_RUN_FIELDS` — как остальные.
- Ошибка Split теперь говорит про оба правила: «Smaller than the minimum
  offcut 40 × 40″ or 10 ft².»
- Замер на тесте 2 владельца: подсказок было 1 на 77 ft², стало **45 на
  544 ft²** — почти половина отхода (1153 ft²) видна как годное стекло.
- Тест «полоска 1/16» проверяет полосы укладчика полос: победителем может
  оказаться раскладка столбиками, где сквозной рез идёт между стёклами, и
  проверка по одинаковому y даёт ложную тревогу.

`test/cut-layout.js` — 50 проверок, полный набор 794.

## Предыдущая версия 5.55 · 20 сентября 2026 — кусок со стеллажа выбирается для батча прямо из журнала; два батча один кусок не поделят.

## Добавлено: выбрать кусок для батча из журнала · 20 сентября 2026

Владелец: «я могу выбрать стоковый лист для какого-то из батчей — так можно
сделать?» Можно было только из батча (таблица SHEETS); теперь и со стеллажа.

- В журнале у свободного куска — **Use in batch ▾**: меню открытых батчей с
  тем же стеклом, где рез ещё не начат (`stockUiBatches`). Батч, из которого
  кусок вырезан, не предлагается: пока его не порежут, куска физически нет.
- Выбор ставит отметку в таблице SHEETS того батча (`cutSetStock … off:false`)
  и говорит: «S-0000001 → B-0002 · press Build there» с кнопкой Open B-0002.
  Если раскрой того батча уже собран, он сбрасывается — с подтверждением.
- В строке журнала видно **Picked for B-0002** (отмечен) или **In B-0002**
  (уже лежит в собранном раскрое), рядом — Open и **Release** (вернуть кусок
  на стеллаж).
- **Кусок не поделят два батча:** `cutStockClaims` собирает отметки и листы
  всех планов, и `cutStockPieces` прячет занятый кусок от чужих батчей — он
  не появится ни в их SHEETS, ни в What if.

`test/cut-layout.js` — 49 проверок, полный набор 793.

## Предыдущая версия 5.54 · 20 сентября 2026 — сток в резе: отмеченный кусок со стеллажа идёт листом прогона, What if его предлагает.

## Добавлено: сток идёт в рез · 20 сентября 2026

Владелец на журнал стока: «а где опция использовать тот сток — сейчас
выглядит как накопление стока».

- `cutStockPieces(glass,mm)` — куски со стеллажа этого стекла и толщины;
  `cutStockFor` добавляет их листами прогона **только если человек отметил**
  галочку (в `pick.sizes` у строки `off:false`), количество — 1.
- Ключ размера у такого листа — номер куска (`S-0000007`), `cutSheetKey` его
  не переписывает. Кромку такому листу не режут: в `cutRunParams` у ключа
  `S-…` Trim и Border по умолчанию 0 (строка может задать своё).
- Площадь считается своя, поэтому укладчик берёт кусок, только если так
  уходит меньше квадратных футов: замер — 10 стёкол 40 × 44 на листах
  144 × 102 = 204 ft²; с куском 100 1/4 × 96 1/4 = 169 ft² (лист + кусок).
- В таблице SHEETS куски идут последними: номер, размер, Qty 1, «from stock».
- **What if** предлагает до двух неотмеченных кусков: «+ S-0000001 · 100 1/4 ×
  96 1/4″ → −35 ft²», `Use` — сброс с этим куском и Build.
- В журнале (Optimization → Stock) у куска видно «In B-0002», если он уже в
  чьём-то собранном раскрое. Статус куска при этом не меняется: когда он
  окончательно уходит со стеллажа, решаем после ответа владельца.
- Поле под подписями осей листа 28 → 34: у кусков размеры дробные, и высота
  «96 1/4″» обрезалась.

`test/cut-layout.js` — 48 проверок, полный набор 792.

## Предыдущая версия 5.53 · 20 сентября 2026 — журнал стока: Optimization → Stock, что лежит на стеллаже, откуда и когда.

## Добавлено: журнал стока · 20 сентября 2026

Владелец про остатки как «бесплатные листы» в раскрое: «идея хорошая, но не
рабочая пока на производстве, потом к ней придём; сейчас пусть будет учёт тех
стёкол, которые попали в сток, не только стикеры».

- **Optimization → Stock** — пятая вкладка раздела (`views/stock-list.js`,
  `viewStockList`): номер S-…, стекло с толщиной, размер, ft², откуда (батч и
  лист), когда забукан, состояние. Крупные первым, счётчик и сумма ft² в
  шапке, на вкладке — число кусков на стеллаже.
- Кнопки строки: **Sticker** (перепечатать) и **Back to waste**
  (`stockOffcutDrop`) — с подтверждением; если кусок ещё лежит на листе
  раскроя, он уходит и оттуда, лист снова показывает его отходом. Снятые
  видны по галочке «Show N back to waste».
- `stockOffcutRows` / `stockOffcutTotal` — в `production/stock-offcuts.js`;
  экран ничего сам не двигает, только учёт.

`test/cut-layout.js` — 47 проверок, полный набор 791.

## Предыдущая версия 5.52 · 20 сентября 2026 — «What if»: цена обрезки кромки и лишнего размера листа в листах и ft²; недобор собирается на одном листе.

## Добавлено: «What if» и компактность · 20 сентября 2026

Владелец спросил, у предела ли мы. Замеры на 12 случайных батчах и на его
тестах: 30 более глубоких случайных прогонов ни разу не дали меньше листов,
чем наш выбор; на тесте 1 (23 листа) и тесте 2 (50 листов) минимум доказуем —
96 × 55 по два на лист (40 штук → ≥20 листов), 34 × 36 по восемь (240 штук →
≥30 листов). Низкий Used % на неудобных батчах — потолок размеров, не
укладчика: лист на 53,8 % — это два стекла 59 × 67, и больше ничего не лезет.

Где деньги на самом деле (замерено): **обрезка кромки**. На тесте 2 Trim X 7/8
снизу стоит 4 листа (50 → 46, Used 77,4 → 84,1 %), Trim Y — ещё 5 листов, а
34 × 36 стоя это ровно 3 ряда по 34″ = 102″, то есть весь лист.

**Кнопка «What if»** (`cutWhatIfCases`, `cutWhatIfSteps`, `cutWhatIfApply`):
- рядом с Reset у собранного раскроя; считает до пяти прикидок — нулевая Trim
  X / Trim Y / Border X / Border Y (только те, что сейчас больше нуля) и
  выключенные размеры листа (до двух, крупные первыми);
- каждая прикидка — обычный Build с подменёнными параметрами
  (`cutPlanSteps(number, probe)`), но **в базу ничего не пишется**: raскрой на
  экране не трогается;
- таблица показывает листы, ft², Used % и разницу к текущему («−408 ft²»);
  `Use` — сброс с этими параметрами и сразу Build (с подтверждением);
- идёт с той же полоской хода (`cutUiSteps` — общий двигатель для Build и
  What if, у полоски своя подпись). Тест 2 владельца: три прикидки — 7,4 с.

**Компактность.** В `cutScore` при равных ft² и числе листов выбирается
раскладка, где самый пустой лист пустее: «ни в коем случае не размазывай по
листам стёкла — раскрой должен быть супер компактный на весь батч» (владелец,
20 сентября 2026). Процент по батчу от этого не меняется — он зависит только
от числа листов, — но самый пустой лист можно не резать, а перенести в
следующий батч. На 12 пробных батчах раскладки не изменились: победители и
так собирали недобор на последнем листе.

Остатки как «бесплатные листы» в раскрое владелец отложил: «идея хорошая, но
не рабочая пока на производстве, потом к ней придём; сейчас пусть будет учёт
тех стёкол, которые попали в сток». Следующим шагом — журнал стока.

`test/cut-layout.js` — 46 проверок, полный набор 790.

## Предыдущая версия 5.51 · 19 сентября 2026 — Build и Reset, как в Perfect Cut; полоска хода Build; укладчик сам смешивает размеры листов.

## Переделано: Build и Reset, полоска хода, смесь размеров листов · 19 сентября 2026

Владелец: «добавим кнопку полный сброс оптимизации (листов, заполненных
оптимизацией, не будет), а Rebuild сменим на Build; когда будем что-то менять —
будем скидывать и нажимать Build. Perfect Cut не даёт ничего изменить, если
оптимизация не скинута»; «иногда оптимизация идёт, я что-то жду, но не вижу
визуально, что идёт процесс»; «когда есть два варианта листов, оно выбирает
только один, а должно выбирать и тот и тот… иногда выгодно взять 5 листов 130
и 25 листов 144».

**Build и Reset** (`cut-layout.js`, `cut-layout-ui.js`):
- План бывает собранным и сброшенным (`plan.reset`). Собранный — параметры
  закрыты: таблица SHEETS (галочка, Qty, Trim/Border/Min dist, + same size,
  + Add sheet size), Rotate, Reset to Master Data, приоритет и галочка стекла
  в списке; сеттеры отвечают «Reset the optimization to change settings.».
  Правка раскладки руками (перетащить, повернуть, снять, заблокировать,
  удалить лист, перенести, сток) — как была. Min offcut можно менять и в
  собранном: он двигает только подсказки остатков.
- `cutPlanReset` — Reset: листов раскладки нет, остаются только
  заблокированные (с ними и забуканный сток), сток незаблокированных снимается,
  как при пересборке. Кнопка Reset спрашивает подтверждение.
- `cutPlanDraft` — черновик по текущим стёклам и параметрам; раскроя ещё нет —
  экран показывает такой же черновик (в базу не пишется, пока не тронули
  параметр: `cutPlanEditable` заводит его сброшенным). После каждой правки —
  `cutPlanRedraft`. Листов нет — пустой лист первого размера с линиями Trim и
  Border и подписью «N glass · Press Build»: правка отступов видна сразу.
- Отложенных правок (`plan.pending`, жёлтая строка «Settings changed»,
  `cutApply(…, later)`) больше нет; `normalizeCutPlans` убирает `pending` из
  старых планов. Кнопка use first убрана — размеры выбирает укладчик.
- Стекло больше листа: кнопка «+ 130 × 96″ without Trim and Border» — Reset,
  строка размера и Build одним нажатием. «Add and optimize» → «Add and build».
- Батч изменился после раскроя — «Reset, then Build.». Перенос листа в батч со
  сброшенным раскроем: лист без стока не встаёт, его стёкла разложит Build.
- Печать стикеров «By sheet» по умолчанию — только если есть листы
  (`cutPlanLaid`).

**Полоска хода.** `cutPlanSteps` — генератор: после каждого варианта (и после
каждого листа в проходах «лист за листом», `cutPackFillSteps`) отдаёт долю
готового; `cutPlanRun` — то же разом (`cutDrain`). `cutUiBuild` считает
кусками по 40 мс через `setTimeout`, между ними обновляет полоску
(`cutUiProgress`, только DOM); экран раскроя на это время приглушён и не ловит
мышь (`.cut-busy`), клавиши молчат. Cancel (`cutUiCancel`) — в базу ничего:
план пишется только в конце. `cutBuildDone()` — промис для тестов.

**Смесь размеров листов.** Листы покупают за ft², выбор — по площади
(`cutScore`, как было):
- `cutPackFillSteps(…, mix)`: каждый лист — того размера, где стекло займёт
  бо́льшую долю листа; размер выбирается быстрой пробой (`CUT_FILL_QUICK`,
  64 варианта из 384), полный перебор — на выбранном. Жадный выбор не всегда
  лучше в целом, поэтому проход «по порядку склада», как раньше, остаётся, а
  смешанный (`fill-mix1…2`) идёт рядом.
- Полосы и столбики — ещё и с каждым размером первым (`@130x96`).
- `cutRightSize` — лист, чьё стекло ложится на меньший размер склада,
  переезжает на него; неудачные пробы запоминаются (`miss`).
- Строка «+ same size» (без Trim и Border) — только под стекло больше поля
  обычного листа (`need` в `cutFillBest`): иначе смесь клала бы на неё всё —
  у неё поле больше.
- Qty у размера — сколько листов есть — соблюдается везде.
- Ускорение: порядок стёкол варианта считается раз на лист (`cutFillOrder`,
  кэш), лист забит — остальное сразу в остаток. Тест 2 владельца
  (390 стёкол): 2,6 → 1,6 с; с двумя размерами — 3,5 с.
- Замеры: тест 1 владельца — 23 листа; с 144 и 130 — 22 × 144 + 1 × 130
  (−15 ft²); тест 2 с 144 и 130 — 15 × 144 + 41 × 130 (5083 ft² против 5100);
  на 14 случайных батчах смесь ни разу не хуже лучшего одного размера, на
  девяти лучше (до −214 ft²).

Тесты `test/cut-layout.js` (45): старые правки параметров идут через Reset
(`ctSet`: Reset → правка → Build); новые — Build/Reset (параметры закрыты,
заблокированный лист остаётся, пустой лист с линиями, полоска, Cancel) и
смесь размеров (оба размера, площадь меньше любого одного, Qty соблюдается).
Полный набор 789.

## Предыдущая версия 5.50 · 19 сентября 2026 — укладка столбиками, как X-резы Perfect Cut: тест владельца — 23 листа, как у Perfect Cut.

## Улучшено: укладка столбиками — тест владельца 23 листа · 19 сентября 2026

Точный тест владельца: лист 144 × 102, Trim X/Y 1″, Border 0; 20 1/4 × 100 1/4
— 111, 60 1/4 × 50 1/4 — 30 (в сообщении «× 100 1/4» — опечатка: при 100 1/4
площадь стекла больше 23 листов), 12 1/4 × 50 1/4 — 10. У нас было 24 листа,
у Perfect Cut 23. Нижняя граница по площади — 22,3, значит 23 — лучшее.

Почему жадный «забей лист» проигрывал: стёкла почти во всю высоту поля
(100 1/4 из 101), и задача по сути одномерная — разложить столбики по ширине
143″. Жадный съедает удобные сочетания на первых листах, в конце остаются
неудобные.
- `cutPackColumns(list,stock,paramsFor,fixed,opt)` — как X-резы: стёкла
  собираются в столбики во всю высоту поля (стекло уже столбика — только если
  полоска сбоку 0 или не тоньше Min distance), столбики раскладываются по
  листам от широких к узким (FFD или BFD). `opt.rows` — то же рядами во всю
  ширину; `opt.orient` — как есть, стоя или лёжа. Формы со скосом — не сюда.
- В `cutPlanRun` добавлено 12 кандидатов `cols-*` / `rows-*`; выбирает, как
  всегда, `cutScore`.
- Случайных проходов на больших батчах меньше: до 150 стёкол — три, до 300 —
  два, больше — один (тест 2 владельца, 390 стёкол: 7,7 → 2,6 с).
- Итог: тест 1 — 23 листа (Used 95,4 %; у Perfect Cut 97,89 % — там большой
  остаток последнего листа 41 1/4 × 101 не считается отходом, у нас он отход,
  пока его не взяли в сток); тест 2 — 50 листов, как было и как у Perfect Cut.
- Подпись узкого высокого стекла идёт вдоль длинной стороны; раньше
  «1 · 20 1/4 × 100 1/4″» налезала на соседей. Короткая подпись — через
  `cutFitLabel`.
- В `CUT_FILL_RANDOM` поправлен комментарий: правильных вариантов 384, не 288.
- Тесты правок руками и мыши раскладывают стёкла в ряд сами (`ctRow`): маленький
  батч теперь может лечь столбиком (больше целый остаток).
- `test/cut-layout.js` — 44 проверки, полный набор 788.

## Предыдущая версия 5.49 · 19 сентября 2026 — укладчик сильнее: случайные варианты к правильным порядкам, три прохода.

## Улучшено: укладчик против Perfect Cut · 19 сентября 2026

Владелец сделал «одинаковый замер» у нас и в Perfect Cut: у нас 24 листа, у
Perfect Cut — 23.

Что проверено на пробных батчах (101–160 стёкол, лист 144 × 102):
- Отступы как в проекте Perfect Cut (Trim 3/4, Border 0) против наших 7/8 со
  всех сторон на этих батчах листов не сэкономили — дело в укладчике.
- Не помогли: другой критерий выбора листа («сначала большие неудобные»),
  «добить хвост» (раскидать стёкла самого пустого листа по другим), выбор листа
  с просмотром на лист вперёд.
- Помогли случайные варианты: на каждом листе к 288 правильным вариантам
  `cutFillRandom` добавляет 64 случайных — 16 порядков стёкол (случайный и «по
  площади с шумом») × 4 способа укладки (`CUT_FILL_RANDOM`). Случайность
  повторяемая: `cutRandom` (mulberry32) с постоянным зерном; три прохода с
  зёрнами 1, 2, 3 — кандидаты `fill1…fill3`, лучший выбирает `cutScore`.
- Итог: A 17 → 16 листов (Used 82,9 → 88,1 %), B 16 → 15 (80,4 → 85,8 %),
  C 14 → 13 (83,1 → 89,5 %); пересборка 0,7–1,1 с.
- Открыто: сравнить на настоящем батче владельца и спросить, какие Trim, Border
  и Min dist стояли в Perfect Cut на этом замере.
- `test/cut-layout.js` — 43 проверки, полный набор 787.

## Предыдущая версия 5.48 · 18 сентября 2026 — цифры раскроя плитками: все на месте, ровно, без наложений.

## Исправлено: цифры над раскроем · 18 сентября 2026

Владелец: «цифры над оптимизацией сделай корректнее — они выглядят как хаос». Первая
попытка свела всё к одной цифре — владелец: «а остальные данные куда дел?
Количество данных было нормально, отображение некрасивое, друг на друге — верни
обратно, но сделай красиво».

- Плитки `cutTile`: значение сверху, подпись мелко снизу, одна линия, тонкие
  разделители. Все одного размера, различие только цветом: использование (Used %,
  Used) — зелёное, отход (Scrap, Net, Net %) — красный, остальное — чёрное:
  «одинакового размера, только для использования зелёный цвет, для вейстеджа
  красный, и всё — не нужно играть с высотой шрифтов».
- **All sheets** (`cutSumTotal`) — во всю ширину строки с кнопками: Used %, Used,
  Scrap, Net, Net %, Sheets, Pieces, To stock.
- **Этот лист** (`cutSumSheet`) — в шапке листа рядом с листом: Used %, Used,
  Scrap, Net; размер и число стёкол — там же.
- Старые карточки `cutStatsBar` убраны; числа с разделителем тысяч и одним знаком
  после запятой (`cutNum`).

## Предыдущая версия 5.47 · 18 сентября 2026 — экран раскроя по макету владельца: список со своей прокруткой, лента листов под листом, приоритет 0, стикер стока — с листом.

## Переделано: экран раскроя по макету владельца · 18 сентября 2026

Владелец прислал макет (PDF «Group 18»): «слева список бесконечный, если большое
количество стекла — пусть будет скролом… справа экран оптимизации, пусть будут
равны; снизу стоковая часть тоже скролом, базово видно первые 5 по размеру;
базово увеличить экран; листы и типы перенести под вид оптимизации; скрол листов
тоже сразу же под оптимизацию; приоритизация базово 0 у всех; когда выбираю
оффкат в сток — не нужно его печатать, он будет печататься следуя листам;
визуализация максимально лаконичная и функциональная, как у Apple».

- **Две колонки 35 / 65** — «правую сторону увеличь, а левую с G number сожми
  плотнее… та инфа слева не так важна, как та справа». Слева список стёкол со
  своей прокруткой, высотой с правую часть, плотный (заголовки групп и таблицы
  прилипают, длинный клиент обрезается). Справа: цифры листа и
  прогона → лист → **лента листов** (прокрутка вбок, номер, штук, Used %; на неё
  можно бросить стекло; ‹ › по краям) → склад листов и параметры.
- **SHEETS:** у каждой строки размера свой **Min dist** (как на макете) —
  `row.minDist` в `cutRunParams`; цифры размера — под названием, use first и
  «+ same size» — столбиком; общий Min dist из панели убран.
- **Остатки** внизу: подсказки и сток вместе, крупные сверху, видно пять, остальное
  прокруткой.
- **Полный экран по умолчанию** (`cutFull = true`); Exit full screen / Esc — обычный вид.
- **Приоритет по умолчанию 0 — «нет»**, 1 — самый срочный … 10 (`cutPriority`,
  `cutPrioRank`, `cutPrioWeight`); в списке пустое поле с «—».
- **Стикер стока не печатается кнопкой «в сток».** Он печатается вместе со
  стикерами листа: при печати «By sheet» всего батча после стёкол листа идут его
  куски в стоке (`stkBatchJobs`, тип задания `stock`). Перепечатать — Print sticker
  в таблице остатков.
- `test/cut-layout.js` — 42 проверки, полный набор 786.

## Предыдущая версия 5.46 · 18 сентября 2026 — кнопка Rebuild: правки склада и параметров ждут пересборки.

## Исправлено: раскрой не пересобирается сам · 18 сентября 2026

Владелец (после нового укладчика батч 76002 — 27 листов вместо 33): «бьёт по
глазам, что я меняю размер листов, и оно не спрашивает… пусть человек думает,
что он контролирует»; «пусть будет кнопка пересобрать: я изменил и нажал
пересобрать, и оно пересобирает; например, заблокировал лист, нажал
пересобрать — и заблокированный лист стал первым».

- `cutApply(number, later, soft)`: правки склада и параметров прогона
  (`cutSetStock`, `cutSetParam`, `cutResetParams`, `cutAddSameSize`,
  `cutRemoveSameSize`, «+ Add sheet size» из таблицы SHEETS) с `later` не
  трогают раскладку: значения запоминаются, линии, подсказки остатков и цифры
  пересчитываются, план помечен `pending`. Экран всегда вызывает их с `later`;
  без него (API, тесты) — пересчёт сразу, как раньше.
- Кнопка **Rebuild** (была Re-optimize) подсвечена, пока есть изменения; сверху
  «Settings changed — press Rebuild. Locked sheets stay and go first.»
- Стекло, которое после правки Trim или Border оказалось за линией, до пересборки
  обведено красным (`data-cut-out`).
- Сразу, без Rebuild: Min offcut (только подсказки остатков), приоритет,
  количество 0; «Add and optimize» в блоке «No sheet size» раскладывает сразу.
- `test/cut-layout.js` — 40 проверок, полный набор 784.

## Предыдущая версия 5.45 · 18 сентября 2026 — укладчик «забить лист», удаление листа, второй лист того же размера, % по размерам, перенос в открытый батч, полный экран.

## Переделано по первой проверке владельца на настоящем батче · 18 сентября 2026

Владелец прогнал батч 76002 (110 стёкол) и прислал «базовые косяки»:

1. **Укладчик слабый.** «Что это за ужас оптимизации… оптимизация должна быть в
   первую очередь, чтобы использовать меньшее количество сквер фит и листов…
   ложить стёкла близко друг к другу по горизонтали и вертикали, и если нужно
   поворачивать — поворачивать на 90 градусов»; «в первую очередь максимально
   забить лист и оставить максимально мало вейстеджа». Пример владельца: 144 × 102,
   девять 50 × 30 (шесть стоя тремя столбиками и три лёжа), 93,75 ft².
   Причина: укладка рядами — первое стекло задавало высоту ряда, остальные
   ложились в него «лёжа» с пустой полосой 20″ сверху; четыре стратегии рядами
   давали почти одно и то же.
   Сделано: `cutPackFill` — лист за листом. Для каждого листа 288 вариантов
   гильотинной укладки (`cutFillSheet`: свободные прямоугольники, каждый рез
   сквозной): порядок стёкол (6), какой поворот пробовать первым, куда класть —
   в т. ч. «смотрит вперёд»: сколько таких же стёкол ещё ляжет в кусок в этом
   повороте, как резать остаток (4), рядами или столбиками. Берётся вариант,
   где на лист легло больше стекла. Старые стратегии рядами остались
   кандидатами — хуже, чем было, не станет. Выбор между кандидатами (`cutScore`):
   все заказы → меньше ft² листов → меньше листов → срочные на ранних листах →
   больший остаток. Если есть срочные — ещё кандидат, где лист берёт их первыми.
   Итог: пример владельца — 9 на лист (91,9 %); 46 × 50 × 30 — 6 листов вместо 8;
   смешанный батч — 12 листов вместо 14–17.
2. **Второй лист того же размера без Trim и Border** («стекло овер»):
   `cutAddSameSize` — строка `130x96#2` в складе прогона, сразу с нулями, своё
   количество, use first, ×. Если стекло не влезает между линиями ни на один
   лист, сверху подсказка с кнопкой «+ 130 × 96″ without Trim and Border».
   Такой лист дозаполняется и обычными стёклами (владелец: «можно и так»).
3. **% по каждому размеру листа:** у строки склада — листов, Used %, Waste %, ft²
   отхода.
4. **Подпись куска в стоке вылезала за кусок** («визуально очень плохо»):
   `cutFitLabel` — одна строка, две, мельче или поперёк узкого куска; подписи
   рисуются поверх.
5. **Удалить лист** (`cutSheetDelete`): кнопка у листа и правая кнопка по пустому
   месту; стёкла — в «Not on a sheet», сток — в отход, листы перенумерованы.
6. **Перенос листа в уже открытый батч** («процесс сложный… если можно упростить —
   нужно упростить»): «Move to batch ▾» — новый батч или открытый батч с тем же
   стеклом, где рез не начат (`cutMoveTargets`); лист встаёт туда последним,
   история «Added from B-…».
7. **Полный экран** («вынеси оптимизацию в другой html»): кнопка ⤢ Full screen —
   меню и шапки прячутся, лист крупнее, Esc — обратно. Отдельная вкладка не
   сделана намеренно: данные в одном хранилище браузера, две вкладки затёрли бы
   правки друг друга.

Минимальный остаток по умолчанию остаётся 40 × 40 (владелец тестировал 20 × 20).
`test/cut-layout.js` — 39 проверок, полный набор 783.

## Предыдущая версия 5.44 · 18 сентября 2026 — Split одним резом до линии Border; лист можно не резать — его стёкла в новый батч.

## Исправлено: Split одним резом · 18 сентября 2026

Владелец по картинке: «ты предлагаешь сделать вырез для стока 40 на 40, то есть
не используешь остальные части стекла… 40 на 40 — это минимальный размер, а не
базовый; всё, что вне, можно использовать — экстендед размер прям до края
бордера». Прежний Split вырезал прямоугольник двумя резами из угла остатка, по
краям оставались полоски в отход.

- `cutStockSplit(number, glass, sheet, index, axis, size)`: один рез, как на
  столе — по длине (`length`) или по ширине (`width`). Кусок в сток получает
  вторую сторону остатка целиком — до линии Border; он от стороны нуля, у стёкол.
  Остаток к краю листа снова подсказка, если не меньше 40 × 40 (бывает и больше —
  сливается со свободным местом рядом).
- Кусок меньше минимума не идёт в сток: «Smaller than the minimum offcut 40 × 40″.»
- Меню и таблица показывают, что уйдёт в сток: «[длина] × 64 1/4″» и
  «98 1/4″ × [ширина]».
- Попутно: у стратегий раскладки заработала настройка ориентации (больше штук в
  полосу / шире / выше) — раньше все стратегии выбирали ориентацию одинаково,
  и два одинаковых стекла ложились одно стоя, другое лёжа. Лучшую раскладку
  по-прежнему выбирает `cutScore`: меньше ft², потом больший остаток.

## Реализовано: лист не режем — стёкла в новый батч · 18 сентября 2026

Владелец: «когда на последнем листе большое пространство, мы его не режем, а
потом те стёкла переносим в другой батч, в новый»; решает человек — «есть такая
переменная как день ожидания»; новый батч — сразу.

- `glassBatchMove(number, pieceIds)` (`erp/production/glass-batches`): запись в
  старом батче закрывается (`releasedAt`, `movedTo`), в новом открывается запись
  с тем же Glass ID; история «Moved to B-…» / «Created from B-…»; заказы остаются
  Batched, `batchNo` — новый батч. Начатый рез не переносится.
- `cutMoveSheet(number, glass, sheet)`: лист уходит из раскроя старого батча, в
  новом батче появляется раскрой с этим листом — та же раскладка; сток с листа
  переезжает (запись `S-…` получает новый батч), лист со стоком остаётся
  заблокированным, без стока — нет (придут заказы — пересчёт дозаполнит место).
  Если раскрой не был устаревшим, он и после переноса не помечается устаревшим.
- Экран: кнопка **Move to new batch** у листа и пункт правой кнопки по пустому
  месту; спрашивает подтверждение; после переноса — зелёная строка «Sheet N ·
  K glass → B-…» и ссылка **Open B-…**. В составе батча перенесённое стекло —
  «Moved to B-…», батч, откуда ушло всё, — «Moved».
- `test/cut-layout.js` — 34 проверки, полный набор 778.

## Предыдущая версия 5.43 · 18 сентября 2026 — остатки в сток: все куски от 40 × 40 на листе, номер S-0000001, стикер Stock offcut, Split по правой кнопке.

## Реализовано: остатки в сток · 18 сентября 2026

Ответы владельца: «ты только подсвечивай участки, которые могли бы пойти в сток,
но пока я не нажму и не отправлю в сток — он идёт как вейст»; в сток — только
руками, внизу табличка «какие размеры и какой лист», кнопка «в сток», потом стикер
(«номер стока, размер и тип стекла»), и только после этого убирать из отходов;
показывать все остатки листа («оба, конечно») и дать разделить на нужные размеры —
«сплит прямо в интерфейсе по правой кнопке»; номер `S-0000001` подходит.

- **Подсказки** `sheet.offcuts` (`cutFreeRects`): все свободные куски не меньше
  минимума (40 × 40″), не пересекаются друг с другом и со стеклом. Считаются
  отходом. На схеме — серые «Offcut W × H».
- **В сток** (`cutStockTake`, `cutStockBook`): кусок получает номер `S-…`
  (`src/erp/production/stock-offcuts.js`, `DB.stockOffcut`, счётчик
  `DB.stockOffcutSeq`), ложится в `sheet.stock`, вычитается из брака и сразу
  печатается стикер. Лист блокируется: кусок забукан с этого места, пересчёт его
  не сдвинет. На схеме — зелёный с номером.
- **Split** (`cutStockSplit`): от подсказки отрезается кусок нужного размера — от
  угла ближе к нулю, у стёкол, — и идёт в сток; не влезает — «Does not fit in this
  offcut.»; можно ввести и повёрнутым.
- **Back to waste** (`cutStockCancel`): кусок снимается со стока, запись остаётся с
  `status: cancelled`, номер повторно не выдаётся — стикер мог уже уйти на стеллаж.
- Кусок в стоке — препятствие для стекла (`cutTaken`: перенос, укладка, подъезд к
  краю). Если лист со стоком разблокировали и пересчитали — его сток снимается,
  `cutPlanRun` возвращает `cancelled`.
- **Меню правой кнопки:** по подсказке — To stock и Split (поля length × width,
  Enter); по стоку — Print sticker, Back to waste.
- **Таблица внизу** «Offcuts · N in stock · M possible»: у подсказки To stock и
  Split, у стока — номер, Print sticker, Back to waste; «Print stock stickers · N».
- **Стикер Stock offcut** — новый тип в конструкторе Master Data → Stickers:
  блоки Stock number («STOCK S-0000001»), штрихкод, стекло, размер крупно,
  площадь, From batch · sheet · дата. Печать — текущим шаблоном и форматом.
- Прежняя отметка `keepOffcut` из v5.41 заменена бронью с номером.
- Импорт JSON: `stockOffcut` не массив, плохой номер, повтор — отказ.
- `test/cut-layout.js` — 32 проверки, полный набор 776.

## Предыдущая версия 5.42 · 18 сентября 2026 — раскрой под мышь: перетаскивание по листу с тенью, правая кнопка, двойной клик, клавиши.

## Переделано: раскрой под мышь · 18 сентября 2026

Владелец: «интерфейс не сильно подстроен на работу с мышкой — ты работаешь через
код». Что мышкой нужно: «заблокировать лайаут, повернуть стекло левой кнопкой,
вынести стекло с лайаута или занести обратно, заблокировать стекло». Было: по
листу стекло не тащилось вовсе (клик по стеклу, потом клик по месту), клик по
подписи на стекле не выбирал его, правой кнопки и клавиш не было, лист 520 px.

- **Тащить по листу** (`cutUiDown/Move/Up`, pointer events): тень показывает,
  куда ляжет — синяя можно, красная занято; липнет к линиям листа и краям соседей
  ближе 1 1/2″ (`cutUiSnap`). После броска стекло подъезжает к краю (`cutCompactSheet`).
- **Бросить** на вкладку листа — перенос туда; в список — снять с листа. Из
  списка на лист — с тенью, стекло центром к курсору.
- **Клик** по стеклу (и по подписи — стекло теперь группа `<g data-cut-piece>`)
  выбирает; **двойной клик** или значок ⟲ на выбранном стекле — поворот; клик по
  пустому месту снимает выбор. Двойной клик ловим сами: после первого клика экран
  перерисован, браузерный dblclick не доходит.
- **Правая кнопка** — меню: Rotate, Take off, Lock glass, Move to sheet N,
  Lock sheet; по остатку — Offcut to stock / back to waste.
- **Клавиши:** R — повернуть, Delete — снять, L — закрепить, Esc — снять выбор
  или закрыть меню (не срабатывают в полях ввода).
- **Наведение** подсвечивает стекло и на листе, и в списке.
- Лист на всю ширину правой колонки; координаты мыши → дюймы через
  `getScreenCTM`, не через ширину рамки.
- `test/cut-layout.js` — 26 проверок; две новые водят настоящей мышью Playwright.
  Полный набор 770.

## Предыдущая версия 5.41 · 18 сентября 2026 — честные цифры раскроя: выбор по квадратным футам, Used % главный, остаток — только подсказка.

## Исправлено: сток не делает процент красивым · 18 сентября 2026

Владелец: «ты ставишь стоковое стекло наравне с заказовым… моя задача
оптимизировать заказы под хороший процент, а не хорошо коллекционировать стекло
в стоке. Максимальная важность — порезать заказы, потратив меньшее количество
стекла, а стоковые стёкла — это просто бонус». Раньше раскладка выбиралась по
NetScrap, где любой остаток от 12 × 12″ сам считался стоком и вычитался из
отходов, — процент выглядел хорошим за счёт склада.

Ответы владельца (18 сентября):
1. «Мы покупаем листы за каждый сквер фут» — выбираем по площади листов.
2. Остаток только подсвечивается; пока его не отправили в сток — это отход.
3. В сток — только руками: таблица остатков внизу, кнопка, потом стикер
   (номер стока, размер, тип стекла); из отходов убирать после того, как
   забукано в сток. — Стикер и номер — следующим этапом.
4. «Главный процент — это сколько использовано».
5. Последний лист с большим пустым местом решает человек: бывает, срок
   поджимает и режут и плохой лист. — Перенос деталей в новый батч — отдельным этапом.
6. Показывать все хорошие остатки и дать с ними работать (Split по правой кнопке).
7. Склад: остатки потом идут в раскрой первыми.
Остаток не меньше 40 × 40″.

Сделано в этой версии:
- `cutScore`: все заказы разложены → меньше квадратных футов листов → меньше
  листов → только потом больший остаток.
- `keepOffcut` у листа: остаток вычитается из брака, только если отмечен.
  На схеме неотмеченный остаток серый «Offcut», отмеченный — зелёный
  «To stock»; у листа кнопка **Offcut to stock**, внизу таблица с галочками.
- **Used %** = used / площадь листов; сток его не меняет. Главная цифра —
  крупно, первой; в шапке «used N %».
- Минимальный остаток по умолчанию 40 × 40″; старое умолчание 12 × 12
  переводится один раз (`DB.cutting.v = 2`).
- `test/cut-layout.js` — 24 проверки, полный набор 768.

## Предыдущая версия 5.40 · 18 сентября 2026 — после правки руками стёкла подъезжают к краю или к соседу; поворот двигает соседей.

## Исправлено: дыра после поворота · 18 сентября 2026

Владелец на настоящем батче: «сделал ротацию, и между стёклами оказалось какое-то
пространство непонятное». Rotate крутил деталь на месте, соседи стояли — стала
уже на 14″, рядом осталась дыра 14″. Владелец: «нужно, чтобы второе стекло
подвигалось к ближнему краю или бордеру другого стекла — мы же говорим про
оптимизацию».

- `cutCompactSheet`: после поворота, снятия, переноса и укладки стёкла листа
  сдвигаются влево, пока не упрутся в линию Trim Y или в соседнее стекло (у форм —
  с Min distance). Только влево и только в своём ряду по высоте — ряд остаётся
  рядом, лист режется сквозными резами. Закреплённые стёкла и заблокированный
  лист не двигаются.
- Поворот: стала шире — стёкла справа в её ряду отодвигаются, если на листе есть
  место; нет места — «No room to rotate on this sheet.», ряд не тронут; закреплённое
  стекло на пути — отказ. Поворот, оставляющий в ряду полоску тоньше Min distance,
  не проходит.
- Попутно: деталь больше нельзя унести с заблокированного листа переносом.
- `test/cut-layout.js` — 23 проверки, полный набор 767.

## Предыдущая версия 5.39 · 18 сентября 2026 — размеры листов цеха: заводятся в Master Data → Cutting и прямо с экрана оптимизации.

## Исправлено: «листа нет для оптимизации» · 18 сентября 2026

Владелец слил раскрой и открыл настоящий батч: «захожу в оптимизацию и говорит,
что листа нет для оптимизации, и я не могу найти в Master Data, где его
добавить». Причина: раскрой брал размер листа только из строк поставки стекла
(Master Data → Materials → Glass → Supply points), а они пусты, пока закупка их
не завела. Строка поставки ещё и требует поставщика — ради размера листа это
лишнее.

- **Размеры листов цеха** — `DB.cutting.sizes` (`cutShopSizes`,
  `cutShopSizeAdd`, `cutShopSizeRemove` в `src/erp/masterdata/cutting.js`).
  Размер цеха подходит любому стеклу. Лист хранится лёжа (`130x96`), 96 × 130 и
  130 × 96 — один размер; больше 400″ — отказ «размер в дюймах».
- **Порядок у стекла** (`cutSheetOptions`): сначала размеры из его поставок (один
  размер от двух поставщиков — одна строка), за ними размеры цеха. Размер цеха
  берётся, когда поставок с размером нет, или кнопкой **use first**.
- **Экран оптимизации.** Если у стекла батча нет ни одного размера — жёлтый блок
  «No sheet size · 6CLEAR» с полями length × width и кнопкой **Add and optimize**:
  размер уходит в Master Data → Cutting и раскрой сразу пересчитывается. Работает
  и для смешанного батча: стекло с размером раскладывается, стекло без размера
  видно в блоке, его детали — в «Not on a sheet». В таблице SHEETS — строка
  **+ Add sheet size**.
- **Master Data → Cutting → Sheet sizes:** все размеры (из поставок и цеха), у
  каждого свои Trim и Border, у размеров цеха — × для удаления (их отступы
  уходят вместе с ними, если размер не приходит из поставок). **Reset to shop
  table** размеры не трогает — это не параметры реза.
- Импорт JSON: `cutting.sizes` не массив — отказ «Cutting sheet sizes must be an
  array.»; мусор, повторы и слишком большие размеры чистятся.
- `test/cut-layout.js` — 22 проверки, полный набор 766.

## Предыдущая версия 5.38 · 18 сентября 2026 — Trim и Border как четыре края листа, свои у каждого размера; без полосок тоньше Min distance; лист лёжа, ноль слева внизу.

## Исправлено: что такое Trim и Border · 18 сентября 2026

Владелец: «ты перепутал трим на листе и бордер к детали», и объяснил, как в
Perfect Cut:

- **Trim X** — отступ от края листа по горизонтали **снизу**;
- **Trim Y** — отступ по вертикали **слева**;
- **Border X** — отступ по горизонтали **сверху**;
- **Border Y** — отступ по вертикали **справа**;
- «и это не даёт деталям налазить на эти линии».

Зачем Border: «деталь может оптимизироваться очень близко к правому краю, отступ
будет 1/16. Машина обозначит этот рез и сделает, но для производства 1/16 — это
ошибка: сломать нельзя, слишком маленькое расстояние. Отправлять на полировку —
это время. А если это LowE — это брак, его нельзя полировать».

Раньше BORDER из его таблицы стоял зазором между формами со скосом («Shape gap»),
а Trim был одинаковым со всех четырёх сторон. Теперь:

- **Рабочее поле листа** (`cutUsable`) — между четырьмя линиями: слева Trim Y,
  снизу Trim X, справа Border Y, сверху Border X. Раскладка, ручная правка,
  полезный остаток и схема считают от него.
- **Зазор между формами** — только **Min distance** (Perfect Cut `Min.Dist.Z`);
  у прямоугольников по-прежнему 0.
- **Полоски внутри полосы.** Та же беда, что у края: в полосе 60″ деталь
  59 15/16″ оставила бы полоску 1/16″. Правило `cutSliverOk`: разница высот в
  полосе либо 0, либо не меньше Min distance. У краёв это держат линии Border.
- **Свои у каждого размера листа.** «Trim X Y Border для каждого размера
  индивидуальные… лучше сделать их отдельными и флексибл». На экране прогона
  четыре поля стоят в строке каждого размера (SHEETS: 144 и 130 правятся
  отдельно); в Master Data — таблица «Edges by sheet size». Порядок: размер в
  этом прогоне → весь прогон → размер в Master Data → толщина.
- **Цифры таблицы** выровнены по рекомендуемым: 6 мм Trim 7/8 (было 3/4 из его
  проекта). 15 и 19 мм в таблице одной цифрой: Trim 0, Border 3 и 4. Min distance
  в таблице нет — берём минимальный Border (в проекте на 6 мм Min.Dist.Z = 3/4).
  Владелец подтвердил оба пункта: у 15 и 19 мм Trim 0; Min distance — минимальный
  Border. Ноль — слева внизу: «ноль слева, всегда так на чертежах принято».
- **Линии — сплошные векторы**, через весь лист, как идёт рез; пунктиров на схеме
  нет: «в будущем для раскроя на машине, чтобы она считала линии правильно».
  Полоса у края залита светло-оранжевым. Лист лёжа (длинная сторона — X), ноль
  слева внизу, подписи осей X и Y; полезный остаток — зелёный «To stock».
- Попутно: правки прогона не доходили до ручной укладки и схемы (группа не знала
  о `sheetPick`) — теперь группа хранит `pick`. Печать схемы писала размер
  первого листа на всех листах смешанного прогона — теперь размер своего листа.
- `test/cut-layout.js` — 18 проверок, полный набор 762. Снимки —

## Предыдущая версия 5.37 · 18 сентября 2026 — раскрой как рабочий инструмент: склад листов прогона, правки руками, потери по листам и заказам.

## Переделано: экран раскроя · 18 сентября 2026

Владелец посмотрел первый экран: «что это за убогость — без переменных,
изменений, без показа вейстеджа, без информации на стекле». Прислал два экрана
Perfect Cut и назвал, чего не хватает. Ответы, которые теперь зафиксированы:

- **Зазор между прямоугольниками — 0.** В его проекте Perfect Cut `Trim X 3/4 ·
  Trim Y 3/4 · Border X 0 · Border Y 0 · Min.Dist.Z 3/4`. Зазор и минимальное
  расстояние нужны, «только когда шейп кривой, имеет скос специфичный». Прежняя
  версия ставила зазор между каждой парой деталей — отсюда лишние листы.
- **Потери считаются как у Perfect Cut:** `Used`, `Gross Scrap`, `NetScrap`;
  полезный остаток (самый большой свободный прямоугольник не меньше минимального)
  вычитается из брака; `Used % = used / (used + net)`. Две строки — по листу и по
  всем. «Очень важно видеть количество потерь на каждом листе и в целом»: по ним
  считается цена большого заказа, поэтому есть и **потери по заказу**.
- **Приоритет 1–10** тянет деталь на первые листы, но «основной задачей является
  оптимизация под низший процент»: из нескольких стратегий берётся вариант с
  наименьшим NetScrap.
- **Склад прогона.** «Иногда мы используем 10 листов 130 и 40 листов 144» —
  у каждого размера своё количество, порядок задаётся кнопкой **use first**,
  размер можно выключить. Листы открываются по порядку, пока не кончатся.
- **Обрезка кромки своя у каждого размера листа** (130 и 144 режут по-разному):
  таблица Master Data → Cutting и правка прямо на экране прогона.
- **Все переменные правятся по месту** — «чтобы в моменте никуда не нужно лезть
  в Master Data»: Trim X/Y, зазор формы, минимальное расстояние, минимальный
  остаток, поворот; **Reset to Master Data** возвращает значения по умолчанию.
- **Правки руками:** снять деталь, положить (перетаскиванием или кликом),
  повернуть, перенести между листами, закрепить деталь, **заблокировать лист** —
  при пересчёте он остаётся как есть и идёт первым. Количество 0 убирает деталь
  из реза. Заказ и форма открываются прямо из списка.
- **На детали написано:** клиент, заказ и позиция, номер места, размер, поворот.
- **Много листов.** Батч бывает на десятки листов: до восьми — вкладки, дальше
  стрелки и выбор «Sheet 7 / 43». Проверка гоняет 180 деталей.
- Ветка `claude/cut-layout-ui`; `test/cut-layout.js` — 15 проверок, полный набор 759.

## Предыдущая версия 5.36 · 17 сентября 2026 — свой раскрой батча по листам, параметры реза цеха, печать стикеров «по листу».

## Реализовано: свой раскрой по листам · 17 сентября 2026

Владелец: «будем печатать из нашей оптимизации». Решение от 1 сентября в силе —
раскрой пишем свой, гильотинный, по прямоугольникам; фигура режется из своего
прямоугольника. Ответы владельца 17 сентября: на листе можно смешивать заказы,
лишь бы стекло одно; остаток храним, учёт остатков — отдельным этапом;
инвентаризацию свяжем позже, чтобы списывало сток и предупреждало заранее по
сквер-футам и «осталось меньше 5 листов».

- **Параметры реза — Master Data → Cutting** (`src/erp/masterdata/cutting.js`,
  `DB.cutting`): строка на толщину, TRIM (обрезка кромки листа) и BORDER (зазор
  между деталями), колонки Min и рабочая; плюс минимальный остаток и поворот
  детали. Цифры — базовый сетап Perfect Cut от владельца (картинка 17 сентября):
  3–4 мм 1/2 · 3/4; 5 мм 5/8 · 3/4; 6 мм 3/4 · 7/8; 8 мм 7/8 · 1; 10 мм 1 · 1 1/4;
  12 мм 1 1/4 · 1 1/2; 15 и 19 мм зазор 3 · 4 (TRIM у них владелец уточняет).
- **Две таблицы программы приведены к цеху:** запас по косой стороне
  (`shapeSafetyBorderAuto`) был 16–19 мм → 1/2″, стало 15–19 → **3″**, 3 мм → 1″;
  припуск на полировку 15 мм переведён в полосу 15–19 → 1/2″.
- **Движок** `src/erp/production/cut-layout.js`: гильотинные полосы, деталь
  ложится в ту ориентацию, при которой в полосу входит больше штук; поворот
  выключается в параметрах. Группировка — одно стекло и одна толщина, заказы
  смешиваются. Лист берётся из поставок стекла (`DB.glassSheet`), самый крупный.
  План хранится в `DB.cutPlan` по номеру батча вместе с отпечатком состава:
  изменился батч — экран говорит «re-optimize».
- **Экран** Optimization → Batches → батч → вкладка **Optimization**: Optimize,
  выход в процентах, схемы листов с номерами мест, остаток пунктиром, печать
  схем для стола резки. На демо-батче из двух заказов вышло 3 листа и 77.2 %.
- **Стикеры «по листу»:** в окне печати батча выбор порядка In order / By sheet
  (второй активен, когда есть раскрой), блок **Sheet · position** печатает
  `Sheet 2 · #6`; в базовых шаблонах он выключен.
- Расход листов записан в плане — на него сядет склад, когда дойдём до Inventory.
- Ветка `claude/cut-layout`; `test/cut-layout.js` — 8 проверок, полный набор 752.


## История до 17 сентября 2026

Всё, что старше версии 5.36, живёт в `docs/HANDOFF_АРХИВ.md` — от первой
сборки до стикеров. Документ разрезан 21 сентября 2026: он вырос до 6498
строк, и новая сессия тратила половину чтения на то, чего уже нет на экране.
Читать архив — только когда нужно узнать, почему что-то сделано именно так.
