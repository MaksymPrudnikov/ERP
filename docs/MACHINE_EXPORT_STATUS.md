# Экспорт раскроя для Maver и Disai — рабочий статус

Актуально на 24 сентября 2026. Главный хендофф и правила раскроя —
[`GLASS_ERP_HANDOFF.md`](GLASS_ERP_HANDOFF.md); этот файл фиксирует только
входные данные и критерии завершения **машинного экспорта**.

Сейчас есть **пробная выгрузка одного открытого прямоугольного листа**, но
не готовый производственный экспорт. В интерфейсе батча после Build:
Optimization → `Trial export` → выбрать нужный стол и отдельно скачать
Maver `.ISO` и `.BMP` либо Disai `.dst` и `.sum`. Это сами станочные файлы,
**не ZIP-архив**. Для просмотра передать на соответствующий стол оба файла.
Их **нельзя запускать на рез** до проверки формата и безопасности с оператором.
Дополнительно `Save folder` на поддерживаемых браузерах и с разрешением
пользователя сохраняет пару файлов в папку, названную по номеру батча,
типу стекла и текущей дате. При отсутствии поддержки остаются отдельные
скачивания; это не автоматическая запись на USB или сетевой диск станка.

## Что уже есть

- Владелец передал два экспорта Perfect Cut одного раскроя **6-мм стекла**:
  Maver — 123 пары `.ISO` + `.BMP`, Disai — 123 `.dst` и общий `.sum`.
  Это **123 уникальные схемы**, не 123 листа. Сумма тиражей `{ N #… }`
  Maver и `REPEAT` Disai — **236 физических листов**, совпадает с
  `QUANTITY=236` в `.sum`. С учётом повторов размещено ровно **2788 стёкол**.
  Это эталон числа листов Perfect Cut для данного набора, если входные
  детали, сырьё и настройки ERP действительно одинаковы.
- Получены два дополнительных экспорта, помеченные владельцем как **4 мм**:
  Maver — 5 пар `.ISO` + `.BMP`; Disai — 5 `.dst` и `.sum`. Здесь 5 уникальных
  схем, 5 физических листов и 40 деталей. В Disai-папке с окончанием
  `.prjx` находятся только выходные DST/SUM, не исходный проект Perfect Cut.
  Образцы не содержат подтверждённого фигурного контура Shape.
- Получены два экспорта **10 мм с Shape**: Maver — 4 пары `.ISO`/`.BMP`,
  Disai — 4 `.dst` и `.sum`, всего 4 физических листа и 18 деталей.
  На превью Maver `3.BMP` видны скосы, а в `1.ISO`–`3.ISO` после прямых
  резов идёт особый этап `M14/M20/G103` и наклонные `M9 → G1`. В Disai
  пять размещений Shape ссылаются на `[DB#]` с наклонными сегментами;
  `[IB#]` хранит габарит, но не полный контур, а `BREAKLN` содержит
  только прямые линии разделения. Исходный старый проект Perfect Cut или
  чертежи фигур, вероятно, недоступны — это ограничивает независимую
  проверку контуров.
- Владелец подтвердил единицы **отображения на столах**: Maver показывает
  размеры в дюймах, Disai — в миллиметрах. Это не равнозначно единицам
  числовых команд в файлах. В 4-мм образце Maver записал размер листа
  `VN211=3299`, `VN212=2435`; Disai — примерно `3298.825 × 2435.225` мм.
  Не преобразовывать координаты Maver по виду экрана без проверки формата.
  Владелец отдельно подтвердил, что **оба стола показывают толщину нового
  образца как 4 мм**.
- `src/erp/production/cut-machine-export.js` содержит read-only
  `cutMachineSnapshot(batchNumber)`: группы, исходные размеры листа,
  фактические поля Trim/Border, детали с поворотом и контуром, сток и
  сквозные линии реза с их физическими концами и порядком. Изменение
  скоса Shape при том же габарите теперь помечает план устаревшим. Для
  размещения используется сетка 1/16″, но снимок сохраняет точный контур
  без этого округления и отказывает, если он не помещается в заготовку;
  старые Shape-планы требуют нового Build. Снимок отказывает при устаревшем
  плане, потерянной или повторной детали, неправильной толщине, выходе за рабочее поле,
  пересечении и неразделимом листе. Ручной пустой лист не превращается в
  фиктивную программу.
- `src/erp/production/cut-machine-trial.js` строит пробные `.ISO`/`.BMP`
  для Maver и `.dst`/`.sum` для Disai из проверенного снимка одного листа.
  Кнопка даёт скачать каждый файл отдельно, без ZIP. Это прототип на основе
  присланных образцов, **не подтверждённый контроллерами**. Генератор
  называет Disai-файлы общим именем проекта: `.sum` и `-001.dst`, как в
  предоставленных образцах для односхемного проекта. Он
  отказывает при устаревшем/небезопасном/пустом листе, Shape и занятом stock
  offcut; толщина ограничена 4, 6 или 10 мм. Разные выходные форматы
  требуют двух адаптеров от одного плана, а не переименования расширения.
  Причина отказа видна красной плашкой прямо у кнопок панели (раньше она
  уходила наверх страницы, и кнопки Disai казались «пустыми»).
- BMP Maver (440 × 320, 24 bit, как в образцах): шапка — батч, стекло, лист,
  размер, толщина; на каждом стекле клиент, PO, заказ/строка, полный Glass
  ID, номер, ширина снизу и высота сбоку; пустые места подписаны `WASTE`,
  подсказанные остатки — `OFFCUT`. На мелких стёклах подписи сокращаются.
- `src/erp/production/cut-machine-disai.js` пишет Disai `[SCHEME]` и
  `[BREAKLN]` из того же дерева резов, что нарисовано на экране (v5.79).

## Формат Disai `.dst` — как прочитан (v5.79)

Буквы `X/Y/Z/U/V` в `[SCHEME]` — **не оси стола**, а уровни вложенной
гильотинной схемы; направление реза чередуется. `X` — полосы по ширине от
`TRIMLEFT`; `Y` — ряды в полосе от нижнего края листа; `Z` — куски в ряду;
`U` — в куске `Z`; `V` — в куске `U`. Уровней не больше пяти. Каждая строка —
длина участка в мм; `IBn` — стекло; ведущий зазор пишется строкой без `IB`,
хвостовой остаток не пишется. Повёрнутое стекло — отдельный раздел
`[IB(2500+ID)]` с `ROTATE=1` (в 6 мм `[IB127]` и `[IB2627]` — одно стекло).
Сам стол режет обычные прямые X/Y; `[BREAKLN]` — все линии этой схемы:
вертикальные по X, затем горизонтальные по Y; соседние на одной прямой
склеены, в том числе через поперечный рез; концы на 1 мм не доходят;
левый трим — на всю высоту. Нижний трим продлевается через правый край,
только если за последней полосой остаётся не больше `BORDERRIGHT`.
Проверено на всех 132 схемах образцов 4/6/10 мм: `[BREAKLN]`, выведенный
из `[SCHEME]`, совпал с Perfect Cut в каждой линии (±0,01 мм печати).

Схема строится из дерева резов экрана. Где оно требует шестого уровня
(стопка 12 × 12 рядом с колонкой 24 1/8″ на листе владельца 130 × 96), этот
участок режется как у Perfect Cut: ряды через всю колонку, затем короткий
рез в каждом ряду. Стекло рез не пересекает, куски отхода не уже
Min distance, каждое стекло восстанавливается из схемы точно — иначе файл
не выдаётся. Если порядок хоть где-то отличается от экрана, после скачивания
панель пишет «Disai cuts some waste in a different order than the screen».
Maver остаётся по линиям экрана.

## Что нужно до производственного экспорта

1. Образцы Shape для обоих столов уже получены (10 мм). Разобрать и
   сопоставить их геометрию с нейтральным планом; без исходного чертежа
   особенно важна пробная загрузка без реза. Единицы отображения известны;
   ещё нужны точные модели и версии контроллеров, а также подтверждение
   единиц внутренних полей файлов.
2. Выяснить кодирование толщины, несмотря на подтверждённые показания столов.
   В образцах 4 мм: Disai `GTHICKNESS=4.000`, Maver `VN216=4`;
   в образцах 10 мм: соответственно `10.000` и `10`.
   В старом образце, который оператор видит как **6 мм**: соответственно
   `152.400` и `152`. Эти значения не подчиняются одному доказанному правилу;
   нельзя выдавать `152.400` за физические миллиметры или умножать толщину
   на 25,4 без проверки формата. Оба контроллера показывают новый образец
   как 4 мм, но это ещё не объясняет код `152`/`152.400` старого 6-мм файла.
3. Сопоставить координаты, начало осей, направление обхода контуров,
   последовательность сквозных резов, Shape и метаданные листа с каждым
   контроллером. Для Shape отдельно проверить этап `M9 → G1` Maver и
   привязку `[DB#]` Disai; `BREAKLN` не является Shape-контуром.
   Превью `.BMP` не заменяет управляющую программу `.ISO`.
4. Расширить тесты адаптеров на известных образцах 4/6/10 мм: число листов и
   уникальных стёкол; размеры, поля и толщину; допустимую геометрию; порядок
   линий; нулевое число непрошедших листов. Отдельно проверить пустой лист,
   поворот Shape и автоматически увеличенный левый Trim Y.
5. Открыть пробные файлы **на обоих контроллерах без запуска реза** и получить
   подтверждение оператора, что толщина, геометрия, начало координат и
   очередность резов показаны верно. Проверка на одном столе не подтверждает
   второй. Только после этого отдельно решать вопрос включения экспорта для
   производства и всего батча.

Для сравнения эффективности с Perfect Cut повторить реальный набор владельца
на 2788 стёкол с идентичными деталями, доступным сырьём, Trim/Border и
Min distance. Эталон из экспортов Perfect Cut — **236 физических листов**;
123 — число разных схем. Числа ERP 240/241/260 из прежних отдельных прогонов
нельзя прямо сопоставлять без сверки входных данных и повторного запуска
после последних исправлений.

Все шесть архивов образцов находятся **вне Git-репозитория**. Их оригиналы
были во временной папке мессенджера; побайтово сверенные локальные копии сохранены
рядом с репозиторием в `../_private-cutting-samples/` на рабочем компьютере
владельца. Не переносить их в публичный репозиторий без отдельного
согласования: они могут содержать сведения о заказах. На другом компьютере
этой частной папки не будет; если нет ни копий, ни оригиналов, попросить
владельца прикрепить архивы снова.
