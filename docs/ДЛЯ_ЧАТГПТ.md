# Инструкция для ИИ, который пишет код для этого проекта

Отдай этот файл целиком ChatGPT (или любой другой модели) **перед** тем, как просить её что-то сделать.
Он написан так, чтобы код приходил сразу пригодным к заливке, без переделок.

---

## Актуальный вход · 17 сентября 2026

Слиты очередь Optimization, компактный Sales, батчи отдельных стёкол с Glass ID
`G-0000001` (номер выдаётся при сохранении заказа, только в производстве, в Sales
его нет) — хендофф v5.29, обзор `docs/GLASS_BATCHES_REVIEW.md`. Справочник причин
NCR «Где + Что» (Master Data → NCR, `DB.ncrReason`) — хендофф v5.30. NCR после выдачи
(кнопка NCR, Remake order, `DB.ncr`, строки в Sales) — v5.31; Recut до выдачи внутри
заказа (`DB.recut`, блок над Notes) — v5.32. Стикеры 4×6 и 3×4 со штрихкодом Code 128, номер юнита `U-0000001`
и конструктор шаблонов Master Data → Stickers — v5.33; после первой печати — выравнивание, готовый размер, свои поля,
✎ Customize для одной печати — v5.34; вместо бордера размер до обработки кромки — v5.35; свой раскрой батча по листам,
параметры реза Master Data → Cutting и печать стикеров «по листу» — v5.36; экран раскроя со складом листов,
правками руками и потерями по листам и заказам — v5.37; Trim и Border как четыре края листа, свои у каждого
размера, без полосок тоньше Min distance — v5.38; размеры листов цеха в Master Data → Cutting и с экрана
оптимизации — v5.39; после правки руками стёкла подъезжают к краю или к соседу — v5.40; честные цифры: выбор по квадратным футам, Used % главный,
остаток в сток только руками — v5.41. Полный набор — 768 проверок. Следующее: своя раскладка по
листам (печать «по листу»), станции со сканером, отгрузка скидами — сначала макет и вопросы владельцу. Даты, номера проверок и указание «следующая PR B»
ниже описывают прежнюю базовую точку; технические правила продолжают действовать.
Хендофф целиком не читать.

## 0. Одно правило важнее всех остальных

**Не переписывай проект. Меняй минимальный связный набор исходных файлов.**

В текущем манифесте 67 браузерных скриптов; система разделена на маленькие модули для точечных изменений.
Обычно это один-два исходника; изменение доменной модели может потребовать ещё UI,
CSS и regression test. Ответ вида «вот новый полный HTML-файл» или «вот
переписанный с нуля модуль мунтина» — брак, даже если код хороший: он затирает
чужие правки и ломает связи.

Если у агента есть Git/workspace — он правит исходники в репозитории, собирает
`dist` и показывает пользователю локальный HTML. Если доступа к файлам нет —
тогда отдаёт полное содержимое каждого заменяемого файла.

---

## 1. Что это за проект

ERP для стекольного производства, заменяет австралийскую систему Spil Glass.
Владелец — не программист, работает через готовые экраны, не через код.
Масштаб: 10–100 заказов в день, один-два цеха.

Репозиторий: https://github.com/MaksymPrudnikov/ERP
Живая версия: https://maksymprudnikov.github.io/ERP/
Управляющий документ проекта: `docs/GLASS_ERP_HANDOFF.md` — там вся история решений.

**Актуальная проверенная точка 15 сентября 2026:** `main` после PR #80 и
автосборки `95d5dd9`, **608** проверок по `src` и столько же по собранному
`dist`. С 9 сентября добавлены: бланки (окно Documents: Work order, Proforma,
Confirmation, Quote), оплаты клиентов (экран Finance), статусы заказа с замком
строк после батча, квоты с ревизиями Q-…-R1, список Sales с фильтрами колонок,
итогами и On Hold. Полная и свежая карта файлов — `КАРТА-ПРОЕКТА.md`, раздел
«Карта кода».

**Следующая работа — `docs/ЗАДАЧА_PR-B.md`, после неё `docs/ЗАДАЧА_PR-C.md`.**
Задания самодостаточные: хендофф целиком (почти 5000 строк) читать не нужно.

**Как работать с владельцем.** Он не программист и говорит по-русски; интерфейс
программы — только по-английски. Прежде чем делать — обсудить подход и дождаться
ответа. Новое показывать макетом на настоящей программе, чтобы он понял на 100%
и не одобрил то, что не понял. Доказательство готовой работы — скриншоты
собранного файла, не диффы.

---

## 2. Технические рамки (нарушение = код не заработает)

Приложение — **не React и не Node runtime**. Это набор обычных браузерных
скриптов, которые подключаются подряд тегами `<script src>` и собираются в один
HTML-файл. Node используется только как инструмент сборки и Playwright-тестов.

**Нельзя:**
- `import` / `export` / ES-модули — файлы подключаются как classic scripts, `import` бросит ошибку
- `require()` — в браузере его нет
- TypeScript, JSX, декораторы, аннотации типов
- npm-пакеты, CDN-ссылки, любые внешние зависимости — файл обязан работать при открытии с диска
- сборщики: webpack, vite, esbuild, babel
- `<link>` или `fetch` на внешние адреса

**Можно и нужно:**
- обычный ES5/ES6 JavaScript в глобальной области видимости
- `function имя(){}` и `const/let/var` на верхнем уровне файла — они видны всем остальным файлам
- шаблонные строки, стрелочные функции, `Array.map/filter/reduce` — это всё поддерживается

**Порядок подключения важен.** Файл выполняется в порядке из `build/manifest.json`:
core → modules/shape → modules/muntin → erp → `erp/storage.js` последним.
Обращаться к функции из файла, который идёт позже, можно только **внутри функции**
(она вызовется потом), но не на верхнем уровне при загрузке.

---

## 3. Карта файлов: что где лежит

Каждый файл — одна способность. Меняй тот, который отвечает за проблему.

### Общее

| Файл | За что отвечает | Что там внутри |
|---|---|---|
| `src/core/dim.js` | разбор и печать дюймов | `inch`, `frac64`, `dimIn`, `fabParseDimStrict` |
| `src/core/poly.js` | геометрия многоугольника | `fabSegCross`, `fabPolySelfIntersects`, `fabSignedArea`, `fabEdgeBounds` |

### Модуль Shape (форма стекла) — `src/modules/shape/`

| Файл | За что отвечает | Что там внутри |
|---|---|---|
| `consts.js` | константы: режимы уклона, типы углов, цвета рёбер | `SS_MODES`, `SS_CORNERS`, `SS_ORDER`, `ssMode`, `ssCornerCount`, `ssLetter`, `ssNum`, `ssNN` |
| `model.js` | нормализация модели, карта угловых рёбер E/F/G… | `ssIsModel`, `ssNormalize`, `ssModel`, `ssEdgeLen`, `ssEdgeMap`, `ssSyncExtra`, `ssCornerTotals` |
| `contour.js` | построение замкнутого контура, лесенки углов | `ssEdgeLocal`, `ssBase`, `ssPointAt`, `ssSpan`, `ssStairs`, `ssContour` |
| `validate.js` | проверки размеров, локтей, угловых блоков | `ssValidate`, `ssWarnings` |
| `geometry.js` | адаптер модель → контур + габарит | `shapeGeometry`, `fabOutlinePoints` |
| `metric.js` | метрический слой внешнего контура без DOM/SVG | `shapeMetricAnnotations`, `shapeMetricFormat` |
| `index.js` | **публичный контракт** | `defaultSmartModel`, `newSmartShapeDef`, `shapeDefToLine`, `smartShapeResult`, `ShapeModule` |

### Модуль Muntin (раскладка перемычек) — `src/modules/muntin/`

| Файл | За что отвечает | Что там внутри |
|---|---|---|
| `catalog.js` | профили баров: ширина лица, глубина, цвета сторон | `MUNTIN_DEFAULTS`, `MUNTIN_BARS`, `normalizeMuntinProduct`, `muntinProduct` |
| `model.js` | нормализация модели раскладки | `clampBars`, `normalizeMuntinModel`, `muntinColors`, `parsePosList` |
| `layout.js` | equal-clear на сетке 1/16″, редкий размер просвета у центра | `MUNTIN_GRID`, `muntinSnap`, `equalClearPositions`, `productionGeometry` |
| `clip.js` | обрезка бара реальным контуром, перпендикулярный отступ | `muntinScanIntervals`, `muntinIntervalUnion`, `muntinIntervalSubtract`, `muntinSegmentBand`, `muntinOffsetHoles`, `muntinIsRectOutline` |
| `adaptive.js` | связка раскладки с реальной формой | `productionGeometryForLine` |
| `bom.js` | имена деталей и список раскроя | `muntinSegId`, `muntinCutList`, `muntinEdgeModeNote` |
| `index.js` | **публичный контракт** | `defaultMuntinModel`, `newMuntinDef`, `realMuntinResult`, `MuntinModule` |

### Оболочка ERP — `src/erp/`

| Файл | За что отвечает |
|---|---|
| `data.js` | справочники, дефолтные данные, нормализация (`DB`, `DEFAULT`, `ROLES`, `SKILLS`, `normalizeStations`, `normalizeUsers`) |
| `icons.js` | SVG-иконки (`ICONS`, `ico`) |
| `views/sales-list-ui.js` | список Sales: колонки, фильтры колонок, итоги, выбор строк, меню правой кнопки, On Hold |
| `views/documents-ui.js` · `documents/*` | окно Documents: бланки заказа и квоты, печать, PDF, Email; Master Data → Company |
| `views/finance.js` · `finance/data.js` | оплаты клиентов, депозит, баланс заказа, CSV для QuickBooks |
| `nav.js` | меню, маршрутизация, `render`, `esc`, `raw`, `fail` |
| `views/dashboard.js` | главная: карта ERP, KPI, дорожная карта |
| `views/users.js` | сотрудники, станции, покрытие навыков |
| `views/sales.js` | оболочка домена Sales и заказов |
| `views/sales-shape-ui.js` | редактор формы, секция «Мунтин бар», раскладка на общем чертеже |
| `views/sales-service-sets-ui.js` | таблица строк заказа: единственная `salesOrderLines`, Effective Production UI |
| `views/sales-line-metrics-ui.js` | Columns, Pricing rules, детализация area/weight/price и Print заказа |
| `views/optimization.js` | очереди Optimization и Shipping: Verify, Unbatch, готовность, выдача |
| `views/glass-batches.js` · `production/glass-batches.js` | очередь стёкол To batch, реестр Batches, состав, история, Unbatch стекла |
| `views/production.js` | поток по уровням, станции, CRUD |
| `storage.js` | localStorage, экспорт/импорт JSON, старт приложения |

### Sales и справочники — `src/erp/`

| Файл | За что отвечает |
|---|---|
| `masterdata/glass.js` / `glass-catalog.js` | каталог стекла, stock/pre-order, spacer/gas/seal/interlayer |
| `sales/data.js` | SalesOrder · order-scoped Makeup · OrderLine · laminated schema |
| `sales/orders.js` | поведение черновика, строки, Excel paste, обработчики Makeup |
| `sales/line-metrics.js` | Actual/Rounded/Billable Area, полный вес и коммерческая цена строки |
| `sales/makeup-ui.js` | Lite / Cavity / Laminated UI |
| `sales/service-sets.js` | Edgework Sets и Effective Production |
| `sales/lifecycle.js` | статусы заказа и квоты, замок строк после батча, переходы с предупреждениями |
| `sales/quotes.js` | ревизии квоты Q-…-R1, отправка по Email, срок цен |
| `shopfloor/data.js` | станции, операции, рабочие места, терминалы |

### Куда идти по симптому

| Жалоба | Файл |
|---|---|
| бар обрезан не по контуру, торчит за стекло | `modules/muntin/clip.js` |
| неправильные просветы, бары стоят неровно | `modules/muntin/layout.js` |
| чертёж мунтина кривой, размеры налезают | `erp/views/sales-shape-ui.js` |
| неправильные длины в списке раскроя | `modules/muntin/bom.js` + `adaptive.js` |
| угловой блок строится не так | `modules/shape/contour.js` |
| принимает заведомо неверную форму / ругается на верную | `modules/shape/validate.js` |
| неправильно понял введённый размер | `core/dim.js` |
| экран неудобный, кнопки не там | соответствующий `erp/views/*.js` |
| в интерфейсе осталось русское слово | соответствующий `erp/views/*.js` — интерфейс только по-английски, языкового слоя нет |
| выбор стекла / Cavity / Laminated работает не так | `erp/sales/data.js` + `orders.js` + `makeup-ui.js` |
| расходятся area, weight, Triple/Large или Unit/Line Price | `erp/sales/line-metrics.js` + `views/sales-line-metrics-ui.js` |
| надо добавить станцию, операцию или рабочее место | `erp/shopfloor/data.js` |
| надо изменить роль | `erp/data.js` |

---

## 4. Контракты — их нарушать нельзя

Модули соединяются только через эти две функции. Всё остальное внутри модуля можно менять свободно.

```js
ShapeModule.compute(shapeDef)
// вход:  {id, name, w:'48', h:'36', smart:{elbowsOn, A, B, C, corners, extraEdges}}
// выход: {valid:true,  width, height, area, points:[[x,y],…], segs, base, warns, line, geometry}
//        {valid:false, reason:'текст ошибки', errors:[…]}

MuntinModule.compute(shape, mdef)
// вход:  shape — определение фигуры (тот же объект, что уходит в ShapeModule.compute)
//        mdef — временный объект {id, name, shapeId, flipped, muntin:{enabled, productId, layout, production}}
// выход: {valid:true,  geo, M, count, totalLengthIn, verticalSegments, horizontalSegments}
//        {valid:false, reason:'текст ошибки'}
```

`shapeMuntinGeoForDraft` переводит сохраняемый `shape.muntin` в формат `mdef`
общего ядра. `newMuntinDef` остаётся фабрикой этого временного объекта; это
не отдельная коллекция БД. Строка заказа хранит `shapeRef`, отдельного
`muntinRef` больше нет.

Для Triple камера выбирается в редакторе: `shape.muntin.cavityIndex` = `0`
(наружная) или `1` (внутренняя). Не задано — выбор ещё не сделан; не подставлять
нулевую камеру при нормализации старого JSON. Для Double камера единственная.
`salesSheetMakeupHTML(makeup, shape)` показывает бар в соответствующей камере
с названием профиля, шириной, глубиной и цветами сторон.

### Четыре запрета

1. **Shape — единственный источник размеров стекла.**
   В ядро Muntin **не добавлять** собственные Width/Height, не считать габарит внутри мунтина.
   Мунтин получает уже готовую фигуру и работает по её реальному периметру.
   Второй источник геометрии = расхождение размеров = брак в цеху.
   Это проверяется автоматическим тестом.

2. **Не переписывать геометрию v4.5 «заодно».**
   Расчёты Shape и Muntin основаны на рабочем Glass Configurator v4.5.
   Согласованное исключение 7 сентября 2026 — центральное распределение редких
   просветов; эталонные длины реза сохранены.
   Улучшать их можно только отдельной задачей, с явной просьбой владельца, и с обновлением
   эталонных чисел в тестах. Тихо «оптимизировать» — нельзя.

3. **Модуль не знает про деньги.**
   В `modules/shape/*` и `modules/muntin/*` не должно быть ни цен, ни клиентов, ни заказов.
   Развязка сделана специально, чтобы модуль можно было чинить, тестировать и показывать отдельно.

4. **Не имитировать работающий обмен с оптимизатором.**
   По решению 1 сентября 2026 свой оптимизатор запланирован после БД и настоящих
   заказов. Существующий экран — прототип, его неактивные действия нельзя выдавать
   за готовую интеграцию. Порядок работ — БД, оптимизатор, станции по логину;
   подробности — в хендоффе. Очередь заказов на экране Optimization (PR B) —
   это статусы заказов (Verify, батч, выдача), а не обмен с оптимизатором.

---

## 5. Эталонные числа — по ним проверяется, что ничего не сломалось

Точка `main` после PR #80 проверена 15 сентября 2026: **608** проверок по
исходникам и **608** по собранному HTML. Они сохраняют геометрические эталоны,
метрический Production Drawing и отдельно проверяют повреждённые данные,
импорт/XSS, отсутствие русского в интерфейсе, Sales/Makeups, коммерческие
показатели строки, бланки, оплаты, статусы, квоты, список Sales и мобильный
viewport. Ключевые значения:

| Проверка | Ожидаемый результат |
|---|---|
| прямоугольник 48 × 36 | площадь 1728 in², 4 точки контура |
| трапеция 48 × 36 при C = 30 (сторона D строится автоматически) | площадь 1584 in², 4 точки |
| угловой блок Single, рёбра E = F = 4 | 6 точек, площадь 1712 in² |
| мунтин 2 верт. × 1 гориз. на этой трапеции | 3 сегмента, длины реза **33 7/64″ · 31 1/8″ · 47 1/8″** |
| тот же мунтин на прямоугольнике | длины **35 1/8″ · 35 1/8″ · 47 1/8″**, форма не считается «shape-adaptive» |
| разные Edge inset X и Y | режим кромки откатывается на `axis` и поднимает флаг `edgeModeForced` |
| сетка производства | ровно 1/16″ |
| прямоугольник 50 × 50, 4 вертикальных бара 5/8″, inset 7/16″ | просветы слева направо: **9 5/16 · 9 5/16 · 9 3/8 · 9 5/16 · 9 5/16″** |
| разбор размеров | `48 1/2`, `48-1/2` → 48.5; `12abc` → 0 (ошибка, а не 12) |

Если код меняет эти числа, проверь причину: без согласованного изменения
требований это регрессия. Обновление эталона должно сопровождаться объяснением
изменившегося поведения, а не только правкой ожидаемого числа.

---

## 6. Если добавляешь НОВЫЙ файл

Файл регистрируется **только в `build/manifest.json`**, в массиве `scripts`:
например, `"src/modules/muntin/arch.js"`. Место — рядом с файлами своего модуля,
до `index.js` этого модуля. Затем выполнить `npm run build:web`:
`src/index.html` генерируется из манифеста и `src/shell.html`, вручную список
скриптов в нём не редактируется. Проверка — `node build/check-manifest.js`.
В коммит входят новый исходник, манифест и сгенерированный `src/index.html`.
При согласованном удалении устаревшего модуля убрать его регистрацию и ссылки.

---

## 7. В каком виде давать ответ

Если агент работает только через чат без доступа к репозиторию, для каждого файла нужен заголовок с полным путём и следом полный код файла целиком:

> **`src/modules/muntin/clip.js`**
> ```js
> /* весь файл от первой до последней строки */
> ```

**Обязательно:**
- полное содержимое файла, а не фрагмент и не diff — владелец заменяет файлы целиком
- сохранить существующие имена функций, если не просили их менять: на них ссылаются другие модули
- шапку-комментарий в начале файла оставить и обновить (что делает, что на входе, что на выходе)
- комментарии по-русски, как в остальном коде

**Не надо:**
- присылать патчи, diff, «замени строку 42 на …»
- присылать весь проект или весь HTML
- переименовывать функции «для красоты»
- добавлять зависимости, сборщики, тесты на новых фреймворках
- писать «остальной код без изменений» вместо кода

Если агент имеет доступ к Git/workspace, сделать точечные правки в отдельной
ветке и подготовить PR. Для изменения кода выполнить `node build/build.js`,
`node test/run.js`, `TARGET=dist node test/run.js` и проверку LF/CRLF через
`node build/check-reproducible.js`. Для UI показать локальный HTML. В коммит
включать исходники, соответствующие тесты, документы и сгенерированный
`src/index.html`. **`dist/GLASS_ERP.html` в ветку не коммитить:** локальная сборка
нужна для проверки; перед коммитом вернуть этот файл к версии из базы ветки.
Его обновляет CI после слияния в `main`. Действия с PR и публикацией выполнять
в пределах разрешений владельца в текущей сессии; не запрашивать уже данное
разрешение повторно. Проверка кандидата не означает, что он уже опубликован.

Заливка одной командой: `node tools/prepush.js` — проверки и пуш ветки; без пуша —
`node tools/prepush.js --dry`. Во время prepush исходники не править.
Окончания строк: `git diff --numstat` должен совпадать с
`git diff --ignore-cr-at-eol --numstat`; не совпало — `node tools/fix-eol.js <файл>`.

---

## 8. Что уже решено и не обсуждается

Владелец потратил на эти решения много времени, повторное предложение их пересмотреть — потеря времени.

- Своя система строится **с нуля**, миграция базы Spil отвергнута.
- По решению 1 сентября 2026 свой оптимизатор раскроя запланирован после БД и настоящих заказов; старое решение об обязательной интеграции с Perfect Cut отменено.
- Бухгалтерию **не переписываем** — остаётся во внешней системе.
- Материал = подложка + толщина + покрытие. Закалка — **не** свойство материала, а процесс.
- Слово «модуль» здесь означает обычный файл с кодом. Это **не** формат Claude Skills.
- Сырой SQL как единственный результат не годится — всё должно открываться как рабочий экран.
- Итоги и статистику на главный экран не выносить — только в будущие отчёты.
- Ручных бумажных процедур не предлагать — всё автоматически.
- Хранилище, копии и «риски» не предлагать, пока нет реальных заказов и общего доступа.
- Мелочь, которую владелец может поправить сам на экране, — назвать клики, без PR.

---

## 9. Короткая версия, если некогда читать

> Проект — набор маленьких браузерных модулей без сборщиков и зависимостей.
> Меняй минимальный связный набор исходников, имена публичных функций не трогай.
> `import`/`export`/npm нельзя. Размеры стекла живут только в Shape, в Muntin их заводить нельзя.
> Геометрию без согласованной задачи не менять; центральные просветы уже реализованы.
> Новый файл — в `build/manifest.json`, затем `npm run build:web`.
> Отдельная ветка → проверки → PR; для UI показать локальный HTML. `dist` собирает CI в `main`.
> Раскладка — `shape.muntin`; `+ / −` в шапке, общий чертёж и печать.
> Проверенная точка — `main` после PR #80, 15 сентября 2026, 608 проверок на каждый target, 64 скрипта.
> Следующая работа — `docs/ЗАДАЧА_PR-B.md`, потом `docs/ЗАДАЧА_PR-C.md`. С владельцем — по-русски, сначала макет и его «да».
