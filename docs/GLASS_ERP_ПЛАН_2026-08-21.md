# GLASS ERP — аудит и план работ

**Дата:** 21 августа 2026
**Проверено:** `MaksymPrudnikov/ERP` @ `29b5fea` (merge PR #3 `stage3b-order-makeups`) + живая версия `https://maksymprudnikov.github.io/ERP/dist/GLASS_ERP.html`
**Кто проверял:** Claude, отдельная сессия. Проверка независимая: клон репозитория, локальная сборка, прогон тестов, ручной проход по системе в браузере.

---

## 0. Итог одной строкой

Фундамент собран честно и работает; Stage 3B залит и живая версия его показывает. Но система пока **не может выставить цену, не может передать заказ в цех и не пишет историю событий** — а без этих трёх вещей заменить Spil нельзя. Следующий шаг — не новые экраны, а журнал событий + ценообразование + разузлование строки в компоненты.

---

## 1. Что проверено и реально работает

Всё ниже — не по документам, а руками в живой версии.

### Техническая база
| Проверка | Результат |
|---|---|
| `node build/check-manifest.js` | ✅ 41 модуль зарегистрирован и в сборке, и в dev-режиме |
| `node build/build.js` | ✅ собирает `dist/GLASS_ERP.html` (421 KB), сборка воспроизводимая |
| `dist` в репозитории == сборка из `src` | ✅ совпадает байт в байт (разница только в переносах строк Windows) |
| `node test/run.js` (по `src/`) | ✅ **108 passed, 0 failed** |
| `TARGET=dist node test/run.js` | ✅ **108 passed, 0 failed** |
| GitHub Actions, последний push в `main` | ✅ success (21.08.2026 03:31 UTC) |
| GitHub Pages | ✅ живая версия отдаёт актуальный Stage 3B |
| Ошибки в консоли браузера | ✅ ни одной |

### Экраны — открываются все, ни одного падения
Главная · Пользователи · Клиенты · Продажи · Конфигураторы · Оптимизация · Производство.
Склад / Закупки / Отгрузка / Финансы — заглушки «план», как и задумано.

### Сквозной сценарий продажи — прошёл полностью
1. Создал клиента → получил код `C-00001`, карточка полная (контакты, адреса, кредитные условия, legacy-ID для переноса из Spil).
2. Создал Sales Order → авто-номер **76002**, статус Draft, локальный Makeup A (Double: 2 lite + 1 cavity).
3. Makeup Builder: Single/Double/Triple, Vision/Spandrel/Laminated, Low-E/Reflective/Frit/Uncoated, spacer/gas/seals, surface #1…#6. Аккордеоны стартуют закрытыми, открыта максимум одна секция — **как и задумано, проверено в DOM (`details[open] = 0`)**.
4. Ввёл размер 48 × 36 → сохранился как `width16: 768`, `height16: 576`. **Канонические 1/16″ работают.**
5. Кнопка `+ Shape` → открылся Production Shape configurator, **сам подставил имя `SO · Line 1` и размеры из строки**. 15 типов фигур, обработка кромок, features (отверстия/вырезы/радиусы/hardware/stamp), Production Drawing, Cutting Shape, экспорт: Печать/PDF, Production SVG, Cutting SVG, Machine JSON, Generic DXF. Для 48×36 посчитал: площадь 12.00 ft², периметр 168″, cut size 48×36.
6. «Сохранить ревизию» → **вернулся в Sales**, строка получила `shapeRef: {revision: 1, fingerprint: shp-d305869b}`.
7. Кнопка `+ Muntin` → Adaptive Muntin открылся уже привязанный к этому Shape. Посчитал: 3 сегмента, total cut 117 3/8″, сетка 1/16″, перпендикулярный отступ 7/16″ от реальной кромки.
8. Сохранил → строка получила `muntinRef` с `shapeRevision: 1`.
9. Save Draft → заказ в списке, кнопки «Открыть» и удаление.
10. **F5 → всё на месте.** Клиент, заказ, Shape, Muntin пережили перезагрузку.

### Мобильный режим
Проверил 375 px на всех экранах, включая тяжёлый редактор Sales Order: **горизонтального переполнения нет нигде**. Это реально сделано, не на словах.

### Вывод по фундаменту
Инженерное ядро (Shape/Muntin) перенесено из v4.5 и защищено эталонными числами. Архитектура «один файл = одна способность» соблюдается, робот-сборщик работает, тесты не декоративные. **Это крепкая база.** Дальше речь только о том, чего в ней ещё нет.

---

## 2. Чего нет — блокеры реального использования

Это не баги. Это отсутствующие куски системы. Без них Spil не отключить.

### 2.1 Цены нет вообще ❗ главный разрыв
В схеме заказа есть `currency` и `paymentTerms`, но **нет ни одного поля с суммой**: ни цены за штуку, ни строки, ни итога.
Модель ценообразования у тебя спроектирована подробно (хендофф, раздел 5: себестоимость → наценка → услуги → energy surcharge, минимум 4 ft², округление вверх до дюйма, FX) — но она живёт в **старом справочном прототипе `glass_erp_v3.html` и в новую оболочку не перенесена**.
Следствие: система умеет описать изделие, но не умеет сказать, сколько оно стоит. Котировку выставить нельзя.

### 2.2 Заказ упирается в тупик
`src/erp/sales/data.js:12` → `const SALES_ORDER_STATUSES=['draft'];`
Единственный статус — Draft. Нет confirmed, нет ACK, нет release в производство. Заказ некуда двигать, и ничто ниже по потоку его не читает. Экран «Производство» управляет станциями, но не видит ни одного заказа.

### 2.3 Разузлования в компоненты нет ❗ самый дорогой долг
Твой же хендофф (раздел 8) говорит прямо: `component ≠ строка заказа`, одна строка триплекса = 3–4 физических компонента, каждый со своим маршрутом, **«если этого нет в модели с первого дня — переписывать придётся всё»**.
Сейчас: строка → Makeup (описание пирога) → и всё. Физические детали не материализуются, маршрута у них нет, трекинга нет.
Чем дольше это откладывать, тем дороже. Это не экран — это ядро модели.

### 2.4 Журнала событий нет ❗ теряется безвозвратно
Раздел 6 хендоффа требует: каждое действие пишет событие (кто, что, когда, сколько, над чем). Отчёты «сколько закалили в июле» читаются **из истории событий**, а не из текущего состояния таблиц.
Сейчас не пишется ничего. Каждый день без журнала — это день истории, который задним числом не восстановить. Это единственный пункт в списке, который **дорожает от простоя**.

### 2.5 Экрана справочников (Master Data) нет
Каталог стекла зашит в `src/erp/masterdata/glass.js`: **16 позиций, 15 из них 6 мм** + один Generic 10 мм. Spacer 8, газ 2, герметики 3, interlayer 3, frit 2, spandrel 2.
Изменить каталог из интерфейса нельзя — только правкой JSON. Пункт 4 твоих открытых вопросов («в каких толщинах реально доступно каждое покрытие») упирается именно в это: даже когда ты дашь данные, вводить их некуда.

### 2.6 Один пользователь, одна вкладка браузера
Всё лежит в `localStorage`. Нет сервера, нет входа по паролю, нет одновременной работы двух человек, нет бэкапа кроме ручного «Экспорт JSON». Роли на экране есть — доступа они не ограничивают (это честно написано в аудите от 18.08).
Для прототипа это нормально. Для цеха — нет.

---

## 3. Реальные баги и ловушки, которые я нашёл в коде

Мелкие по объёму правок, но неприятные в работе оператора.

| # | Что | Где | Чем грозит |
|---|---|---|---|
| B1 | **Кнопка Close стирает черновик без вопроса** | `src/erp/sales/orders.js:22` — `salesOrderClose()` обнуляет `soDraft` без `confirm()` | Оператор ввёл 50 строк, промахнулся мимо Save Draft → всё исчезло молча |
| B2 | **F5 убивает несохранённый заказ** | `soDraft` живёт только в переменной JS | То же самое, но по случайному обновлению страницы |
| B3 | **Заказ сохраняется со строками без размеров** | `salesOrderSave()` (`orders.js:23`) проверяет только `customerId` | В Draft уезжают пустые строки, ошибка всплывёт уже в цеху |
| B4 | **Excel-вставка ждёт Qty \| Height \| Width, а таблица показывает Width \| Height** | `salesExcelApply()` (`orders.js:69`) | Перепутанные ширина/высота на целой пачке строк — самая дорогая ошибка в стекле |
| B5 | **«● несохранённые изменения» горит всегда** | флаг `dirty` сбрасывается только экспортом JSON (`storage.js:13`, `storage.js:20`) | Оператор думает, что данные не сохранены, хотя они в localStorage. Или наоборот — привыкает и игнорирует настоящее предупреждение |
| B6 | **Весь модуль Sales — только по-английски, даже в RU** | словарь `i18n.js` работает в одну сторону RU→EN; строки Stage 3B написаны сразу по-английски и в словаре отсутствуют («Save Draft», «Order Notes», «ORDER LINES», «GLASS / IGU MAKEUPS») | В русском режиме половина системы русская, половина английская |
| B7 | **Документ обещает то, чего в репозитории нет** | `docs/STAGE3B_FINAL_AUDIT.md`, п.8: «CI now runs on feature-branch pushes and pull requests». Реально в `.github/workflows/build.yml` только `push: branches: [main]` + `workflow_dispatch` | Ветку `stage3b-order-makeups` пришлось проверять ручным запуском. Следующий раз можно забыть и влить непроверенное |
| B8 | **Пользователей ноль** | `DB.user` пустой на старте | Дашборд показывает 0, отчёт по навыкам пустой, привязка станций не на ком проверять |

---

## 4. Где ты остановился

**Фаза 1 «Фундамент» — закрыта по содержанию.** Последнее действие: 21 августа 06:31 влил PR #3 `stage3b-order-makeups` в `main`, CI зелёный, живая версия обновилась.

Что это значит по твоей же дорожной карте (хендофф, раздел 8):
- Ф1 Фундамент — мастер-данные и доменная оболочка ✅ построено (кроме экрана справочников и реального каталога)
- **Ф2 Заказ — здесь ты стоишь сейчас.** Клиенты ✅, шапка+строки заказа ✅, прайсинг ❌, разузлование в компоненты ❌, ACK ❌, чертёж ✅
- Ф3 Цех, Ф4 Планирование, Ф5 Замыкание — не начаты

Ты на границе: дальше начинается не «ещё один экран», а **ядро — цена, компоненты, события**. Именно от них зависит, придётся ли потом переписывать.

---

## 5. Что нужно от тебя

Это данные, которых **нет ни у одного ИИ** — только у тебя на производстве. Пока их нет, любой код будет угадыванием. Заполняй прямо в этом файле.

### 5.1 Станки и печь — блокирует Ф3
```
Печь: минимальный размер детали ______ × ______ in
Печь: максимальный вес садки ______ kg
CNC1 — это уровень 2 (кромка/полировка) или 3 (сверловка/пазы)?  ______
FURN1 — на каком месте маршрута стоит закалка?  ______
```
Полный список станков (сейчас заведено 4, нужны мойка, линия IGU, автоклав, ламинация и остальные):

| Станок | Что делает | Рабочее поле, in | Уровень в потоке |
|---|---|---|---|
|  |  |  |  |
|  |  |  |  |

### 5.2 Каталог стекла — блокирует Ф2 и Ф3
Сейчас засеяно только 6 мм. Нужна таблица: какое покрытие в каких толщинах реально доступно.

| Производитель | Продукт | Код | 3мм | 4мм | 5мм | 6мм | 8мм | 10мм | 12мм | Сток или под заказ | Срок, дн. |
|---|---|---|---|---|---|---|---|---|---|---|---|
| Vitro | Solarban 60 | 6SB60 |  |  |  | ✓ |  |  |  |  |  |
|  |  |  |  |  |  |  |  |  |  |  |  |

### 5.3 Цены — блокирует весь модуль ценообразования
```
Курс USD → CAD: ______   (в прототипе захардкожено 1.37)
Минимальная площадь к оплате по умолчанию: 4 ft² — подтверждаешь?  ______
Для каких материалов/клиентов минимум 2 или 3 ft²?  ______
Наценка по умолчанию, %: ______
Energy surcharge, %: ______
% отхода: ______
```
Плюс реальные цены закупки по материалам — в прототипе везде 0.

### 5.4 Роли и кто видит прибыль — блокирует права доступа
```
Реальные должности в компании: ______________________________
Кто, кроме тебя, имеет право видеть себестоимость и маржу: ______________________________
```

### 5.5 Perfect Cut — блокирует мост в цех
Зайди в настройки Perfect Cut и посмотри, как он подключён к Spil: строка подключения / DSN / ODBC / папка обмена файлами. Или спроси у R.O. SRL напрямую.
```
Механизм: ______________________________
```
**До этого формат моста не проектируем — угадывать бессмысленно.** Так записано в хендоффе, и это правильно.

### 5.6 Эталон для приёмки — нужен перед ценообразованием
**3–5 реальных заказов из Spil** (экспорт или просто скриншоты): шапка, строки, размеры, услуги, итоговая цена.
Без них невозможно доказать, что новая система считает так же. Критерий выхода Ф2 в твоём же плане — «расхождений по цене и составу деталей нет», а сверять не с чем.

### 5.7 Одно решение, которое принимаешь только ты
```
Когда съезжаем с localStorage на настоящий сервер (Postgres + Next.js)?
  ( ) как только заработает цена — тогда Этап 9 идёт раньше
  ( ) после того как модель устоится на прототипе
  ( ) пока не решил
```
От этого зависит, будет следующий большой этап интерфейсом или бэкендом.

---

## 6. План работ

Порядок не случайный: сначала то, что дорожает от простоя, потом то, что блокирует остальное, потом удобство.

### Этап 3C — Заткнуть ловушки (0.5–1 день)
Самое дешёвое и самое заметное. Ничего архитектурного.

- [ ] `confirm()` в `salesOrderClose()`, если в черновике есть строки — **B1**
- [ ] `beforeunload`-предупреждение при несохранённом заказе — **B2**
- [ ] Проверка на Save: строка без Width/Height не даёт сохранить, поле подсвечивается — **B3**
- [ ] Excel-вставка: поменять порядок на `Qty | Width | Height | Mark` (как в таблице) — **B4**
- [ ] Индикатор переименовать: «не выгружено в JSON» вместо «несохранённые изменения» — **B5**
- [ ] Решить один раз: Sales по-русски или система двуязычная не полностью. Если по-русски — добавить строки Stage 3B в `i18n.js` — **B6**
- [ ] `.github/workflows/build.yml`: добавить `pull_request` и push в feature-ветки, чтобы документ не врал — **B7**
- [ ] Засеять 2–3 тестовых пользователя — **B8**

**Файлы:** `src/erp/sales/orders.js`, `src/erp/views/sales.js`, `src/erp/i18n.js`, `src/shell.html`, `src/index.html`, `.github/workflows/build.yml`
**Готово когда:** новые тесты на «Close спрашивает», «строка без размера не сохраняется», «Excel-вставка кладёт ширину в ширину» зелёные, общий счёт тестов вырос.

---

### Этап 4 — Журнал событий (2–3 дня) ⏳ начинать первым из крупных
**Почему первым:** это единственная вещь, которая теряется навсегда. Каждый день без журнала — потерянный день истории.

- [ ] Новый модуль `src/erp/core/events.js`: `logEvent(actor, module, action, entityType, entityId, payload)`
- [ ] Вписать в `build/manifest.json` и `src/index.html` (иначе `check-manifest` остановит сборку)
- [ ] Событие пишется при: создании/изменении/удалении клиента, заказа, строки, Makeup, Shape, Muntin, станции, пользователя
- [ ] Экран «Журнал» — фильтр по дате, модулю, объекту, пользователю
- [ ] События попадают в экспорт/импорт JSON, при импорте не затираются, а сливаются
- [ ] Ограничение объёма: старые события не должны переполнить localStorage (ротация + предупреждение)

**Готово когда:** создал клиента → открыл заказ → сохранил → в журнале три записи со временем, автором и объектом.

---

### Этап 5 — Экран справочников (2–3 дня)
- [ ] Экран Master Data: стекло, spacer, газ, герметики, interlayer, frit, spandrel — полный CRUD
- [ ] У продукта: availability (сток/заказ/спец/неактивно), поставщик, срок поставки, размеры листа — схема уже это предусматривает, экрана нет
- [ ] Защита от удаления позиции, которая используется в Makeup (как сделано для Shape/Muntin)
- [ ] Загрузить **твой реальный каталог** из п. 5.2

**Готово когда:** ты сам, без меня и без JSON, добавляешь новое покрытие в 8 мм и сразу выбираешь его в Makeup Builder.

---

### Этап 6 — Ценообразование (4–6 дней) ❗ ключевой для Ф2
Перенос раздела 5 хендоффа из справочного `glass_erp_v3.html` в модульную оболочку.

- [ ] Таблицы: `fx` (курсы), `cost` (цена закупки по материалу + точке поставки + дате), `servicePrice`, `treatPrice`, `settings`
- [ ] `costOf(material)` — последняя цена по дате, конверсия по FX, + % фрахта
- [ ] `priceLine()` — округление размера вверх до дюйма → площадь факт vs площадь к оплате (минимум 4 ft²) → размер реза с припусками → расход с % отхода → себестоимость → цена продажи → услуги построчно → подытог → energy surcharge → итог × количество
- [ ] Поля суммы в строке и в шапке заказа
- [ ] **Одна функция `можно(пользователь, что, действие)`** — прибыль, себестоимость, % наценки скрываются через неё, а не через `display:none` в вёрстке
- [ ] Скрытие работает и в экспорте, и в печатной форме — иначе цифры уедут Excel'ем
- [ ] `makeupFingerprint(m)` — стабильный отпечаток пирога, считается из **id** (стекло, закалка, поверхность, spacer, газ, герметики по порядку), а не из названий. Ровно как `shp-…` у Shape. Это не библиотека, а вычисляемый ключ: одинаковые makeup в разных заказах схлопываются сами, и появляются отчёты «сколько таких сделали» и себестоимость по пирогу. Можно посчитать задним числом по старым заказам — данные для этого уже сохранены

**Готово когда:** берём один из реальных заказов Spil (п. 5.6) и получаем ту же итоговую сумму с точностью до центов.

---

### Этап 7 — Разузлование в компоненты + маршруты (5–8 дней) ❗ не откладывать
- [ ] `component` как отдельная сущность: строка Double = 2 компонента, Triple = 3, Laminated считается по слоям
- [ ] У компонента: свой номер, размер реза, толщина, обработка, ссылка на строку и на Shape
- [ ] `routing_step` — маршрут компонента по станциям из справочника Production
- [ ] Маршрут выводится из геометрии и обработки (в Shape уже есть кусок этой логики — тест «маршрут выводится из геометрии» зелёный)
- [ ] Экран «Детали в работе»: что за компонент, на какой станции, в каком статусе
- [ ] Каждое движение компонента пишет событие (Этап 4)

**Готово когда:** одна строка триплекса 48×36 разворачивается в 3 компонента с разными маршрутами, и это видно на экране.

---

### Этап 8 — Жизненный цикл заказа (2–3 дня)
- [ ] Статусы: `draft → quoted → confirmed → released`, вместо единственного `draft`
- [ ] Переход `confirmed` замораживает цену и состав; дальше только через ревизию
- [ ] ACK (подтверждение заказа) на печать/PDF — механика печати уже есть в Shape, переиспользовать
- [ ] `released` создаёт компоненты (Этап 7) и отдаёт их в производство

**Готово когда:** заказ проходит все четыре статуса, и на `released` в цеху появляются детали.

---

### Этап 9 — Точка решения по серверу
Зависит от твоего ответа в п. 5.7. Здесь либо начинается Postgres + Next.js (хендофф, раздел 3), либо продолжаем на прототипе.
Признак, что тянуть больше нельзя: **двум людям нужно работать одновременно**, или потеря браузерных данных станет неприемлемой.

---

## 7. Правила, которые не нарушать (проверено — они рабочие)

- **Не присылать новый большой HTML «взамен».** Правится один файл в `src/**`, робот пересоберёт `dist`. Проверено: сборка воспроизводимая, `dist` в репозитории совпадает с локальной сборкой.
- **Новый файл → вписать в `build/manifest.json` И в `src/index.html`, в одинаковом порядке.** Иначе `check-manifest` остановит сборку — это не формальность, он реально ловит.
- **`node test/run.js` до правки и после.** 108 тестов. Если поехало число `33 7/64″ · 31 1/8″ · 47 1/8″` — сломан перенос v4.5, а не тест.
- **В Muntin не добавлять свои Width/Height.** Один источник геометрии — Shape. Защищено тестом.
- **Правка геометрии — отдельным коммитом**, не вперемешку с интерфейсом.
- **Не проектировать формат обмена с Perfect Cut**, пока нет настроек коннектора.
- **Не предлагать миграцию базы Spil.** Решение строить с нуля — финальное.
- **Не возвращать глобальную библиотеку конфигураций.** Проверено арифметикой на текущем каталоге: один Double = `(16 стекло × 3 закалки) × (8 spacer × 2 газа × 3 × 3 герметика) × (16 × 3)` = **331 776 сочетаний**, и это пока только 6 мм. С реальными толщинами, поверхностью покрытия, Frit, Spandrel, Laminated и Triple счёт идёт на миллионы. Заранее создать и назвать их нельзя — оператор утонет в поиске и наплодит дублей. Это та же болезнь, что и плоский список типов стекла «6MM CLR TEMP», только этажом выше. Makeup внутри заказа — не упрощение, а единственный способ не хранить каталог, которого не существует.

---

## 8. Как работать с этим планом

1. Один этап = одна ветка = один PR. Перед вливанием — зелёный CI.
2. Отмечай `[x]` прямо в этом файле по мере закрытия.
3. Когда отдаёшь задачу ИИ (мне, ChatGPT, другой сессии) — давай три вещи:
   - `docs/ДЛЯ_ЧАТГПТ.md` из репозитория,
   - `docs/GLASS_ERP_HANDOFF.md` целиком,
   - **этот файл + номер этапа**, над которым работаем.
4. Данные из раздела 5 заполняй по мере получения — они разблокируют этапы 5, 6 и 7.

---

## 9. Что я бы сделал на твоём месте на этой неделе

1. **Сегодня:** ответить на п. 5.1 (станки и печь) и 5.4 (роли) — это ты знаешь наизусть, десять минут.
2. **На этой неделе:** запросить у Perfect Cut / R.O. SRL настройки коннектора (п. 5.5) — там ответ идёт долго, письмо стоит отправить раньше всего остального.
3. **Параллельно:** выгрузить 3–5 реальных заказов из Spil (п. 5.6). Без эталона ценообразование нечем проверить.
4. **Из кода:** Этап 3C (полдня, сразу заметно) → Этап 4 (журнал событий, потому что он дорожает от простоя).

Каталог стекла (п. 5.2) — большая таблица, её можно заполнять постепенно, она нужна к Этапу 5.
